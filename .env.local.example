# Copy this file to .env.local and fill in real values.
# .env.local is gitignored (see .gitignore's `.env.*` pattern) — never commit
# real secrets into a tracked file. Loaded automatically by the CLI
# (hawkeye/envfile.py); no manual `export` needed once this file exists.

FINNHUB_API_KEY=
ANTHROPIC_API_KEY=

# Task List

進捗管理の唯一の正本（single source of truth）。新規タスクの受付・登録手順は
`hawkeye-task-cycle` Skill（`.claude/skills/hawkeye-task-cycle/SKILL.md`）と
`docs/task-template.md` を参照。1タスクずつ進行し、完了条件を満たすまで
次のタスクに着手しない。

## ID 採番ルール

- 形式は `T-###`（3桁ゼロ埋め、連番）。
- 一度発行した ID は再利用しない。中止・保留になったタスクも欠番のまま
  この表に残し、削除しない。

## カラムの使い分け

- **タスク名**: 10〜40文字程度。そのタスクを端的に説明したものにする
  （`docs/task-template.md` 項目1）。
- **タスク詳細**: 目的・変更範囲・禁止事項など（`docs/task-template.md`
  項目2〜4）をまとめて記述する。長文になってよい。
- **依存**: 他タスクへの依存（`docs/task-template.md` 項目8）。
  `T-001（ブロッカー）`/`T-001（推奨: 理由）`の書式で書く。依存が無ければ
  「無し」と明記し、`-`や空欄のままにしない。

## 状態の選択肢

`未着手` / `調査中` / `実装中` / `ローカル検証済み` / `実環境検証待ち` /
`完了` / `保留` / `中止`

## タスク一覧

| ID | タスク名 | タスク詳細 | 状態 | 進捗 | 依存 | 完了条件 | 証拠 |
|----|----------|------------|------|------|------|----------|------|
| T-000 | (サンプル行) 記載形式の例 | タスクの記載形式の例。実タスク登録時に置き換えるか、新規行を追加する | 未着手 | 0% | - | 完了条件をここに具体的なコマンド/出力で明記する | - |
| T-001 | Judgeルール3・5をEVベースに一本化 | （`docs/design/RETURN_TARGET_STRUCTURAL_GAPS.ja.md` 障害C対応）。**目的**: ルール3（Adversaryの短い主張がBullの長い主張より説得力があればPASS、という弁論の勝敗ルール）が、severity4以上の攻撃を監視条件に転換してもなお機械的にPASSへ誘導しており、実績19件全件PASSという極端な結果になっている。放置すると審理から一切BUYが出ず、年50%目標の達成が構造的に不可能。**変更範囲**: `hawkeye/tribunal/prompts.py`（JUDGE_SYSTEM: ルール3を独立ルールとして撤廃し、「未反証だが転換済みのseverity4以上の攻撃は確信度から相応の割引を行う」という指示にルール5側で統合）／`hawkeye/tribunal/pipeline.py`（`_judge_rule_check`の確信度閾値を0.55→0.65に変更。ロジック自体は変えず定数のみ）／`strategy/VERIFICATION_PROTOCOL.md`（ルール3・5の記述更新、「enforced twice」の記述精度も見直す）／`hawkeye docs tribunal-roles --write`によるドキュメント再生成。**禁止事項**: ルール1（デフォルトPASS）・ルール2（severity4以上の攻撃の未対応チェック）・ルール4（Risk Officerの経済性チェック）はスコープ外。`config.py`の他の閾値（`min_reward_risk`等、障害A関連）は触らない。 | 実環境検証待ち | 100% | 無し | ① `prompts.py`のJUDGE_SYSTEMからルール3の弁論勝負の文言が消え、確信度割引の指示に置き換わっている ② `_judge_rule_check`の確信度チェックが0.65を使っている ③ `hawkeye docs tribunal-roles --check`が通る ④ 既存tribunalテストスイートが全件成功する ⑤ 新設計の挙動を検証する新規テスト（severity4攻撃を転換した上で確信度0.65超→BUY、0.65以下→PASSの両ケース）が追加され成功する<br>**テスト方法**: 単体（ScriptedLLM/StaticProviderでのオフラインテスト）。実環境確認は次回`/hawkeye-run`実行時にJudgeの実際のconviction分布を観察（即時検証不可のため継続観察）。<br>**停止条件**: CLAUDE.mdの共通停止条件のみ。 | 2026-08-17実装。①〜⑤すべて充足。`hawkeye/tribunal/prompts.py`（JUDGE_SYSTEMのルール3を撤廃しルール4へ統合、旧ルール4→3・旧ルール5→4・旧ルール6→5に繰り上げ）／`hawkeye/tribunal/pipeline.py`（`_MIN_BUY_CONVICTION = 0.65`を新設し`_judge_rule_check`が参照）／`hawkeye/reports/tribunal_roles.py`（日本語解説の番号振り直しとルール4の記述差し替え）／`strategy/TRIBUNAL_ROLES.ja.md`（`hawkeye docs tribunal-roles --write`で再生成、`--check`が「✅ prompts.py と一致」を出力）／`strategy/VERIFICATION_PROTOCOL.md`（「enforced twice」→「5つのうちコードで再検算するのは2つだけ」に訂正、旧ルール撤廃の経緯を追記）／テスト4本を新設（`test_converted_severe_attack_keeps_buy_above_the_floor` / `..._overturns_buy_below_the_floor` / `test_conviction_that_cleared_the_old_floor_no_longer_buys` / `test_conviction_exactly_at_the_floor_still_buys`）。`.venv/Scripts/python.exe -m pytest -q` → **778 passed**（失敗0）。commit: 820d30c。**未検証**: 実環境でのJudgeのconviction分布は次回`/hawkeye-run`実行時に継続観察（本タスクでは検証不可）。 |
| T-002 | セクターETF比較材料を審理に追加 | （セッション方式のみ、`docs/design/RETURN_TARGET_STRUCTURAL_GAPS.ja.md` 障害F対応）。**目的**: 審理3役に業界・セクターの比較材料が一切無く、銘柄固有の反応か業界全体の流れかを区別できない。**変更範囲**: 新規セクター→ETF変換表（主要セクターのみ、GICS11セクター相当）／既存OHLCV取得（Yahoo/Finnhub）を再利用したETF値動き取得処理／`hawkeye/contracts/models.py`（`CandidateBrief`への新フィールド追加）／`hawkeye/scout/`配下のdossier組み立て箇所への配線／`hawkeye/tribunal/prompts.py`（新フィールドの説明追記）／`hawkeye/tribunal/casefile.py`（セッション方式のパッケージ出力への反映）。**禁止事項**: 今回はセッション方式のみ対応、API方式（`hawkeye/tribunal/llm.py`経由）への配線はスコープ外（`CandidateBrief`拡張自体はモデル共有により自然に波及しうる点に留意）。主要セクター以外（Finnhubの細かい業種分類）への個別対応はスコープ外。既存`CandidateBrief`の他フィールドの意味は変えない。 | 実環境検証待ち | 100% | T-001（実装のブロッカーではないが、ルール3が直っていないと新しい材料を渡しても効果が判定できないため、着手順としてT-001を先に推奨） | ① 主要セクター（GICS11セクター相当）のETFマッピング表が実装されている ② `CandidateBrief`に新フィールドが追加され、`casefile.write_package()`のセッション方式dossier出力に反映されている ③ 新規ユニットテスト（セクター名→ETFコード変換、ETF値動き計算）が追加され全件成功する ④ 既存テストスイートが全件成功する<br>**テスト方法**: 単体（変換関数・計算関数）＋結合（`build_brief`からdossier出力までの一連の流れ）。実環境確認は次回`/hawkeye-run`実行時。<br>**停止条件**: CLAUDE.mdの共通停止条件に加え、Finnhubの`finnhubIndustry`分類が主要セクターのどれにも当てはまらない業種が多数見つかった場合、対応方針をUser確認。 | 2026-08-17実装。①〜④すべて充足。**停止条件は不発**（実データ19件で届いた業種名13種類は全てGICS11セクターに対応付け可能。`docs/knowledge/MEASUREMENTS.ja.md` に実測記録を追加）。`hawkeye/marketdata/sector_etf.py`（新規。業種名→GICSセクター→SPDRセクターETF の2段変換表。未知の業種は推測せず`None`）／`hawkeye/contracts/models.py`（`SectorContext`モデル新設、`CandidateBrief.sector_context`追加）／`hawkeye/marketdata/snapshot.py`（`build_sector_context()`新設。ETF株価を既存OHLCV経路で取得し`event_stats()`を再利用、候補との差分`excess_*`まで算出。取得失敗・業種未対応・カタリスト日欠落はいずれも`None`を返し候補は落とさない）／`hawkeye/tribunal/prompts.py`（共通前提に読み方を追記、Adversaryの`macro_regime`に業界便乗の攻撃指針を追加）／`hawkeye/reports/tribunal_roles.py`＋`strategy/TRIBUNAL_ROLES.ja.md`（日本語解説を追記し再生成、`--check`が「✅ prompts.py と一致」）。新規テスト32本（`tests/test_sector_etf.py`）。`.venv\Scripts\python.exe -m pytest -q` → **810 passed**（着手前778 + 32、失敗0）。**スコープからの逸脱1件**: dossier組み立ての実体は`hawkeye/scout/`ではなく`hawkeye/marketdata/snapshot.py`の`build_brief()`だったため、配線はそこに入れた（`scout.py`もCLIも同関数を通るので両経路に届く）。**未検証**: 実環境でETF株価が実際に取得できるか、審理3役が新材料をどう使うかは次回`/hawkeye-run`実行時に観察。 |
| T-003 | 決算解説文から乖離原因を抽出し検証付きで渡す | （`docs/design/RETURN_TARGET_STRUCTURAL_GAPS.ja.md` 障害D-2の前提／「参考」節のハルシネーション対策）。**目的**: EPSサプライズと売上サプライズが大きく乖離した決算について、その原因（一時的な税効果・評価益なのか、本物のマージン改善なのか）を審理3役が検証する手段が無く、Judge/Adversaryは推測をそのまま事実であるかのように書いている（PGY・UNHの実例で確認済み）。障害D-2を正しい順序（原因分析→OK/NG判定）で実装するための前提であり、放置するとD-2は「理由を見ずに形だけで機械的に減点する」という誤った設計のまま実装されるリスクがある。**変更範囲**: 新規`hawkeye/scout/quality_extraction_agent.py`（仮。`guidance_agent.py`と同型のパターン——EarningsWhispersの`summary`全文を入力、原因に言及した引用文を抽出、4つのハルシネーション防止チェック［引用が原文に実在するか・正しい文脈での引用か・単位が明記されているか・期間が一致しているか］を適用）／`hawkeye/scout/quality.py`（`LegVerdict.excerpt`をEPS/売上レッグにも適用する配線。ガイダンスレッグの既存パターンをそのまま流用）／`.claude/skills/hawkeye-run/SKILL.md`（セッション方式でこの抽出をどのタイミングで走らせるか。ガイダンス抽出と同様の非同期キュー方式が要るか検討）／`hawkeye/tribunal/prompts.py`（新しい`excerpt`をJudge/Adversaryがどう扱うかの指示追記。既存のnull≠zeroの原則をそのまま適用し、原因抽出が無ければ`unverified`として扱う）／**以下4箇所は2026-08-17にUser承認のうえ追加**（ガイダンス読み取りが6箇所に分かれており、当初の4ファイルだけでは完了条件②の「ガイダンスレッグと同じ形」が成立しないため）: `hawkeye/contracts/stocks.py`（読み取り結果の保存先として`EarningsPrint`に新フィールド。JSON payload保存のためDB移行不要）／新規`hawkeye/scout/cause_case.py`（仮。`guidance_case.py`と同型の、読み取り待ち行列をファイルで持つモジュール）／`hawkeye/cli.py`（待ち行列を出す／読み取り結果を受け取る2コマンド。`hawkeye guidance queue`/`submit`と同型）／`hawkeye/scout/scout.py`（走査から待ち行列への積み込み配線）。**設計決定（2026-08-17 User承認）**: 実行時期は走査直後・全候補（ガイダンス読み取りと同じstep 2bに並べる。乖離の大小で絞り込まない——実測18件中ほぼ全件が乖離形のため削減効果が小さく、しきい値を`config.py`に足すと不変条件7の承認が別途要る）。抽出役のAIにEPS・売上のサプライズ率は見せない（見せると「理由があるはずだ」という前提が生まれ、書かれていない原因を作文させる誘因になる。ガイダンス抽出の情報遮断と同じ扱い）。**禁止事項**: 決算資料本体（プレスリリース・10-Q等）の新規取得はスコープ外（障害B3として別途検討済み・優先度低）。抽出対象は必ずEarningsWhispersの`summary`のみ。抽出結果でEPS/売上の実績値・サプライズ率を書き換えることは禁止（不変条件1・不変条件6に抵触。あくまで理由の注記であり、数値の訂正ではない）。 | 実環境検証待ち | 100% | T-005（ブロッカー: 抽出した原因を審理3役へ届ける経路そのものが壊れており、直さない限り本タスクの成果が3役に到達しない。2026-08-17に解消済み）／T-001（実装のブロッカーではないが、ルール3が直っていないと新しい材料を渡しても効果が判定できないため、着手順としてT-001を先に推奨） | ① 新規抽出エージェントが実装され、`guidance_agent.py`の`parse_reply()`と同等の4項目チェックを持つテストが全件成功する ② EPS/売上レッグの`LegVerdict`に`excerpt`が設定される経路が、ガイダンスレッグと同じ形で動作することを確認する単体テストが追加され成功する ③ 抽出結果が無い場合に`unverified`扱いとなり、Judge/Adversary側のプロンプトが「未確認」として扱う指示になっていることを確認するテストが追加され成功する ④ 既存の`test_earnings_quality.py`等の関連テストスイートが全件成功する<br>**テスト方法**: 単体（引用検証ロジック、ScriptedLLMでの疑似応答に対する4項目チェック）。結合（`quality.py`全体の判定フローにexcerptが正しく乗ること）。実環境確認は次回`/hawkeye-run`実行時。<br>**停止条件**: CLAUDE.mdの共通停止条件に加え、セッション方式での実行タイミングが既存のSKILL.md手順と矛盾する場合は実装前にUser確認。 | 2026-08-17実装。①〜④すべて充足。**停止条件は発火し、User確認済み**（実行タイミング＝走査直後・全候補／スコープ拡張4箇所／抽出役への情報遮断／T-005の切り出し、の4点を実装前に確認）。**命名変更**: 仮称`quality_extraction_agent.py`→`cause_agent.py`（待ち行列`cause_case.py`と対にし、`guidance_agent.py`/`guidance_case.py`と同じ並びにするため）。`hawkeye/scout/cause_agent.py`（新規。抽出役への指示文・応答スキーマ・4項目チェック［引用が原文に実在／見通し文とコンセンサス文からの引用を拒否／数値には単位必須／期間は報告四半期のみ］・拒否理由の分類表）／`hawkeye/scout/cause_case.py`（新規。読み取り待ち行列と決算行への書き込み）／`hawkeye/contracts/stocks.py`（`CauseReading`モデル新設、`EarningsPrint.cause`と`cause_reason`追加。JSON payload保存のためDB移行不要）／`hawkeye/paths.py`（`cause_dir()`）／`hawkeye/scout/scout.py`（走査からの積み込み。`_read_guidance`→`_stage_prose_reads`に改名）／`hawkeye/cli.py`（`hawkeye cause queue`/`submit`）／`hawkeye/scout/quality.py`（EPS・売上レッグへの`excerpt`配線＋審理3役向け英文への「NOTE」追記。**スコアと判定は不変**）／`hawkeye/tribunal/prompts.py`＋`hawkeye/reports/tribunal_roles.py`＋`strategy/TRIBUNAL_ROLES.ja.md`（3役共通前提とAdversaryの攻撃指針を追記し再生成、`--check`が「✅ prompts.py と一致」）／`.claude/skills/hawkeye-run/SKILL.md`（step 2b-2を新設）。**付随して直した2件**（本機能が成立しないため）: ①2つの待ち行列が同じ決算行を書き換えるため、先に提出した方が後を弾いていた → 決算行IDの固定をやめ「報告数値が同一か」で判定する`revision.target_row()`を新設（実績値の訂正が入った場合は従来どおり拒否）。②テストの隔離設定がガイダンス用の待ち行列しか指定しておらず、走査を伴う全テストが開発環境の実ディレクトリ`var/cause/`に書き込み、そこからフィクスチャを読み戻していた（実測65ファイル蓄積）→ `tests/conftest.py`で両方を隔離。新規テスト37本（`tests/test_cause_agent.py`）。`.venv\Scripts\python.exe -m pytest -q` → **850 passed**（着手前813 + 37、失敗0）。**実CLIでの通し確認済み**: `hawkeye cause queue` → パッケージ出力（サプライズ率が含まれないことを確認）→ `hawkeye cause submit` → 3役が読む英文に引用が1回だけ載ることを実行して確認。commit: 11eccdb。**未検証**: 実環境で決算専門サイトの文章から実際に原因が読み取れる割合、および3役が新材料をどう使うかは次回`/hawkeye-run`実行時に観察。**既知の制限**: 走査レポートに読み取り待ち件数を出す集計は付けていない（スコアに影響せず順位付けが待たないため。件数は`hawkeye cause queue`で確認する）。 |
| T-004 | Overreaction銘柄スキャナーを新設し審理に統合 | （`docs/design/RETURN_TARGET_STRUCTURAL_GAPS.ja.md` 障害B対応）。**目的**: ドクトリンが認める4種のエッジ源のうち`overreaction`（一時的な問題で質の良い銘柄が過剰に売られる）を検出する仕組みが存在せず、決算サプライズだけでは狙える機会が絞られている。**設計方針（Claude提案・User承認済み）**: 統合方法＝`/hawkeye-run`の日次サイクルに組み込み、決算候補と同じ審理枠を使う。審理枠配分＝1日3枠のうち1枠をoverreaction候補に予約（該当日に入口ゲート通過候補が無ければ3枠とも決算候補に戻す。新エッジ源のデータ蓄積を優先し複雑な横断スコア正規化はしない）。急落の条件＝直近10営業日（決算カタリストの鮮度条件と数字を揃えた）で株価15%以上下落、かつ下落期間の平均出来高が過去3か月平均の1.5倍以上（暫定値。実測データが貯まり次第見直す）。原因特定の基準＝下落期間内に否定的なニュースが見つかった候補のみを対象とし、理由不明の下落はv1では対象外（ドクトリンの`overreaction`定義が「直せる、定量化できる問題」を前提とするため）。既存ソフトゲート＝ATR14≤8%・次回決算7日超はそのまま流用、「決算当日の値動き≤25%」は「下落期間全体の下落率50%超で除外」に置き換え。**変更範囲**: 新規`hawkeye/scout/overreaction.py`（仮。既存OHLCV取得・ニュース取得を再利用した急落検出＋原因特定＋順位付け）／既存の入口ハードゲート（価格・時価総額・ADV20）をそのまま適用する配線／`.claude/skills/hawkeye-run/SKILL.md`（日次サイクルへの組み込み、審理枠1枠の予約ロジック）／候補生成時に`CatalystType.EARNINGS_OVERREACTION`（既存enum）を割り当てる配線。**禁止事項**: API方式への配線はスコープ外（T-002と同様、セッション方式のみ）。決算サプライズ側の既存ロジック（ランキング式・入口ゲート等）は変更しない。急落条件・原因特定基準の数値は実測データが無いまま追加調整しない（暫定値のまま実装し運用データが貯まってから見直す）。 | 未着手 | 0% | T-001（実装のブロッカーではないが、ルール3が直っていないとoverreaction候補が本当に良いエッジ源か判定できないため、着手順としてT-001を先に推奨） | ① 急落検出ロジック（15%下落・10営業日・出来高1.5倍）が実装され単体テストが成功する ② ニュース突合による原因特定ロジックが実装され、原因が見つからない候補が除外されることを確認するテストが成功する ③ 既存ハードゲートと新設ソフトゲート（下落率50%上限・ATR14・次回決算7日）が正しく適用されることを確認するテストが成功する ④ `/hawkeye-run`の日次サイクルでoverreaction候補が1枠を予約し、候補が無い日は決算候補が3枠とも埋めることを確認するテストが成功する ⑤ 既存テストスイートが全件成功する<br>**テスト方法**: 単体（急落検出・原因特定・ゲート適用の各ロジック）。結合（`/hawkeye-run`のサイクル全体でoverreaction候補が審理枠に正しく混ざること）。実環境確認は次回`/hawkeye-run`実行時。<br>**停止条件**: CLAUDE.mdの共通停止条件に加え、`CatalystType.EARNINGS_OVERREACTION`という名称が「決算による過剰反応」を暗示し、決算と無関係な急落（訴訟・製品リコール等）を含む本タスクの設計と食い違う場合、名称・enum設計の見直しをUser確認。 | - |
| T-005 | 順位付け後に審理3役への決算説明文を作り直す | （T-003着手前の調査で発見した不具合。2026-08-17実測で再現確認済み）。**目的**: セッション方式でAIが読み取ったガイダンス（会社自身の次期見通し）が、審理3役（Bull・Adversary・Judge）に渡る決算説明文（`Catalyst.description`。決算の3本柱の結果を英語1段落で説明する文字列で、3役が決算内容を知る主な経路）に一切反映されていない。走査（`hawkeye scout`）がガイダンス未読の時点で説明文を作り、順位付け（`hawkeye rank`）の`rerank_after_guidance()`が点数と3本柱の判定だけ作り直して説明文を放置するため。結果、読み取りが成功していても3役には「Guidance not disclosed — consensus [pending_extraction]」という古い文が渡る。T-003で抽出する乖離原因も同じ経路で3役に届けるため、これを直さない限りT-003は目的を達成できない。**変更範囲**: `hawkeye/scout/scout.py`の`rerank_after_guidance()`（作り直した`quality`から`describe_quality_en()`で説明文を再生成し、候補のブリーフのカタリストに載せ直す）／`tests/test_scout_quality_wiring.py`（新規テスト）。**禁止事項**: 点数式・順位付けロジック・入口ゲートは変更しない。ブリーフの構造化数値（`eps_surprise_pct`・`revenue_surprise_pct`のオーバーライド）は書き換えない（不変条件6に抵触しうるため、説明文のみを対象とする）。API方式の経路（`hawkeye/tribunal/llm.py`）は変更しない。既に台帳に記録済みの推奨（`Recommendation`）には一切触れない（不変条件1）。 | 実環境検証待ち | 100% | T-006（ブロッカー: 2026-08-17の初回実環境確認で、審理3役が実際に読む資料を作る経路が経路Aだけでなく経路B（`hawkeye case open --from-earnings`）にもあり、本タスクは経路Aしか直していないことが判明した。`/hawkeye-run` 手順書 2e が使うのは経路Bであるため、T-006が完了するまで本タスクの目的『読み取ったガイダンスを審理3役に渡す』は達成されず、完了にできない） | ① ガイダンス読み取り成功後に`rerank_after_guidance()`を通すと、候補のブリーフのカタリスト説明文からフラグ`pending_extraction`が消え、読み取り結果（例: `Guidance beat consensus`）が入っていることを確認するテストが成功する ② 読み取り結果が「会社は見通しを出していない」だった場合、説明文がその理由（`no_guidance_in_source`）を示し、「未読」と取り違えられないことを確認するテストが成功する ③ ブリーフの他フィールド（EPS・売上のサプライズ率、スナップショット）が書き換わっていないことを確認するテストが成功する ④ 既存テストスイートが全件成功する<br>**テスト方法**: 結合（走査→ガイダンス添付→順位付けの一連の流れをオフラインのScriptedLLM/StaticProviderで再現）。実環境確認は次回`/hawkeye-run`実行時。<br>**停止条件**: CLAUDE.mdの共通停止条件のみ。 | 2026-08-17実装。①〜④すべて充足。`hawkeye/scout/scout.py`の`rerank_after_guidance()`に、作り直した3本柱の判定から審理3役向けの説明文を`describe_quality_en()`で再生成し、候補のブリーフのカタリストに載せ直す処理を追加（`model_copy`で説明文のみ差し替え、ブリーフの他フィールドは触らない）。新規テスト3本（`tests/test_scout_quality_wiring.py`: `test_the_tribunal_is_told_the_guidance_that_was_read_after_the_scan` / `test_a_company_that_guided_nothing_says_so_to_the_tribunal` / `test_rewriting_the_paragraph_touches_nothing_else_on_the_brief`）。**RED→GREENを確認**: 修正前に前2本が`AssertionError: 'pending_extraction' not in ...`で失敗することを実行して確認した後に実装。`.venv\Scripts\python.exe -m pytest -q` → **813 passed**（着手前810 + 3、失敗0）。commit: f25be5e。**未検証**: 実環境で審理3役が更新後の説明文をどう使うかは次回`/hawkeye-run`実行時に観察。 <br>**2026-08-17 実環境確認の結果: 未達**。`/hawkeye-run` を実走させたところ、3案件すべての `bull.input.json` に `Guidance not disclosed [guidance_not_published]` が書かれていた（`hawkeye rank` はHLIT +13.3% / SDRL +3.0% と採点済み）。原因は本タスクの修正漏れではなく、同じ説明文を作る処理が `hawkeye/scout/single.py` の `judge_ticker` にも存在し、そちらが走査の結果を参照せずカレンダーから作り直すため。本タスクの完了条件①〜④はすべて `rerank_after_guidance()` を直接呼ぶテストで、Userが実際に打つコマンドを一度も実行していなかった（この構造的な原因への対処はT-007）。 |
| T-006 | 案件生成が保存済みの決算読み取りを捨てる問題 | **目的**: 銘柄を指定して審理用の案件を作るコマンド（`hawkeye case open TICKER --from-earnings`）が、走査で読み取り済みの「会社の来期見通し（ガイダンス）」と「決算の理由」を審理3役への資料に載せず、`Guidance not disclosed [guidance_not_published]` / `no explanation ... (it has not been read yet)` と書いている。原因は、この経路が銘柄マスタに保存済みの最新決算行を使わず、決算カレンダーからその場で取り直して新しい行を組み立てるため（`judge_ticker`）。読み取り結果自体は保存されており採点（`hawkeye rank`）には正しく反映されているので、壊れているのは資料を書き出す経路のみ。放置すると、3役は会社が自ら見通しを引き上げた事実を知らないまま審理し、点数を付けた根拠が審理の資料からだけ消えた状態でUserがBUY/見送りの判断を受け取る（金銭判断に直接影響）。2026-08-17実測: 開いた3案件すべてで発生（ガイダンス欄の誤りはHLIT・SDRLの2件、理由欄の誤りは3件全部）。**変更範囲**: `hawkeye/scout/single.py`（`judge_ticker`: 保存済みのactive print rowを優先して使う）／`hawkeye/cli.py`（`_judged_earnings` / `cmd_case_open` の呼び出し側）／対応するテスト。**ユーザー決定（2026-08-17）**: 保存済みの読み取りと取り直した決算数値が食い違った場合は、案件を開かずに停止して人間に報告する（自動でどちらかを採らない）。**禁止事項**: T-005で入れた走査→審理の経路（`hawkeye rank` 後の説明文再生成）は触らない。`record_print` / `revise_print` の実績値訂正の仕組みと既存データの扱いは変えない。`hawkeye/tribunal/prompts.py` の役割プロンプトは触らない。今日開いた3案件（case_5944e2300f48 HLIT / case_95864f27c41b AMBQ / case_af22b31960b6 SDRL）は修正の検証材料として残し、削除しない。 | 未着手 | 0% | 無し（T-005のコード修正は既に入っており、本タスクはT-005が一度も触っていない別経路（`hawkeye/scout/single.py` の `judge_ticker`）の修正なので、着手の前提にならない。依存は逆向きで、T-005が本タスクの完了を待つ） | ① `hawkeye case open HLIT --from-earnings --nav 10000` が書き出す `bull.input.json` に `Guidance beat consensus +13.3%` が含まれ、`guidance_not_published` が含まれない ② 同じく理由欄が `(the source states none)` になり、`(it has not been read yet)` にならない ③ 保存済みの読み取りと取り直した数値が食い違うケースで、案件を開かずに差分を報告して終了する ④ `pytest` が全件成功する ⑤ ③の挙動を検証する新規テストが追加され成功する<br>**テスト方法**: 単体（オフライン。StaticProviderで、保存済み行にガイダンスがある場合・無い場合・数値が食い違う場合の3ケース）／実環境確認は次回の `/hawkeye-run` で①②を実コマンドで確認する。<br>**停止条件**: `CLAUDE.md` の共通停止条件のみ。 | - |
| T-007 | 完了条件にUserが打つコマンドの出力を必須化 | （T-005の実環境確認で判明した、完了条件の書き方そのものの問題への対処）。**目的**: T-005は完了条件①〜④をすべて満たし813件のテストが通っていたが、その4つはいずれも `rerank_after_guidance()` という関数を直接呼ぶテストで、`/hawkeye-run` の手順書が実際に使うコマンド（`hawkeye case open --from-earnings`）を一度も実行していなかった。そのため「関数の論理は正しいがUserが通る経路は直っていない」状態で進捗100%・実環境検証待ちに到達できてしまった。`CLAUDE.md` は 2026-08-02(d) の教訓として「A green test suite is evidence about logic, not about the delivered path」を既に記録しているが、それを完了条件の書式として強制する仕組みが無い。放置すると、同じ「テストは緑だが運用経路は壊れている」タスクが今後も完了扱いで積み上がり、Userは直ったと理解したまま誤った資料で金銭判断を行う。**変更範囲**: `docs/task-template.md`（項目5「完了条件」に、Userまたは手順書が実際に実行するコマンドとその出力を最低1つ含めることを必須化する記述を追加）／`.claude/skills/hawkeye-task-cycle/SKILL.md`（§1 Intake と §5 完了報告に同じ要求を反映）。**スコープの明示（未確認のため要確認）**: グローバル側の `~/.claude/skills/task-cycle/SKILL.md` と `~/.claude/rules/common/goals.md` は本タスクの変更範囲に**含めない**。Hawkeye固有の運用として先に入れ、他プロジェクトへ広げるかは別途判断する。**禁止事項**: 既存タスク（T-001〜T-006）の完了条件を遡って書き換えない（記録の改変になるため）。テストコードそのものには手を入れない（本タスクはドキュメントと手順の変更のみ）。 | 未着手 | 0% | 無し（T-006とは独立に着手できる。T-006はコードの修正、本タスクは完了条件の書式の修正） | ① `docs/task-template.md` の項目5に、実行コマンドと期待出力を最低1つ含める旨が明記されている ② `.claude/skills/hawkeye-task-cycle/SKILL.md` の §1 と §5 に同じ要求が反映されている ③ T-006の完了条件が新しい書式を満たしていることを、本タスクの完了報告の中で実際に照合して示す ④ `pytest` が全件成功する（ドキュメント変更のため既存テストが壊れていないことの確認）<br>**テスト方法**: 実コード変更が無いため単体テストの追加は不要。回帰として `pytest` 全件実行のみ。実環境確認は不要（ドキュメントの記述の有無で判定できる）。<br>**停止条件**: `CLAUDE.md` の共通停止条件のみ。 | - |

# T-007: 完了条件にUserが打つコマンドの出力を必須化

## メタデータ

- 作成日: 2026-08-17
- 更新日: 2026-08-17
- 状態: 完了
- 進捗: 100%
- 依存: 無し（T-006とは独立に着手できる。T-006はコードの修正、本タスクは完了条件の書式の修正）

## タスク詳細

（T-005の実環境確認で判明した、完了条件の書き方そのものの問題への対処）。**目的**: T-005は完了条件①〜④をすべて満たし813件のテストが通っていたが、その4つはいずれも `rerank_after_guidance()` という関数を直接呼ぶテストで、`/hawkeye-run` の手順書が実際に使うコマンド（`hawkeye case open --from-earnings`）を一度も実行していなかった。そのため「関数の論理は正しいがUserが通る経路は直っていない」状態で進捗100%・実環境検証待ちに到達できてしまった。`CLAUDE.md` は 2026-08-02(d) の教訓として「A green test suite is evidence about logic, not about the delivered path」を既に記録しているが、それを完了条件の書式として強制する仕組みが無い。放置すると、同じ「テストは緑だが運用経路は壊れている」タスクが今後も完了扱いで積み上がり、Userは直ったと理解したまま誤った資料で金銭判断を行う。**変更範囲**: `docs/task-template.md`（項目5「完了条件」に、Userまたは手順書が実際に実行するコマンドとその出力を最低1つ含めることを必須化する記述を追加）／`.claude/skills/hawkeye-task-cycle/SKILL.md`（§1 Intake と §5 完了報告に同じ要求を反映）。**スコープの明示（2026-08-17 User確認済み: Hawkeye専用のみ）**: グローバル側の `~/.claude/skills/task-cycle/SKILL.md` と `~/.claude/rules/common/goals.md` は本タスクの変更範囲に**含めない**。Hawkeye固有の運用として先に入れ、他プロジェクトへ広げるかは別途判断する。**禁止事項**: 既存タスク（T-001〜T-006）の完了条件を遡って書き換えない（記録の改変になるため）。テストコードそのものには手を入れない（本タスクはドキュメントと手順の変更のみ）。

## 完了条件

① `docs/task-template.md` の項目5に、実行コマンドと期待出力を最低1つ含める旨が明記されている ② `.claude/skills/hawkeye-task-cycle/SKILL.md` の §1 と §5 に同じ要求が反映されている ③ T-006の完了条件が新しい書式を満たしていることを、本タスクの完了報告の中で実際に照合して示す ④ `pytest` が全件成功する（ドキュメント変更のため既存テストが壊れていないことの確認）<br>**テスト方法**: 実コード変更が無いため単体テストの追加は不要。回帰として `pytest` 全件実行のみ。実環境確認は不要（ドキュメントの記述の有無で判定できる）。<br>**停止条件**: `CLAUDE.md` の共通停止条件のみ。

## 証拠

2026-08-17実装。①〜④すべて充足。スコープは着手前にUser確認済み（Hawkeye専用のみ。グローバル側の `~/.claude/` は対象外のまま）。① `docs/task-template.md` 項目5に「Userまたは運用手順書が実際に実行するコマンドと期待出力を最低1つ含める」ことを必須化（テスト全件緑はロジックの証拠であって運用経路の証拠ではない旨・T-005の実例・実環境でしか打てないコマンドは誰が・いつ実行するかの明記要求を含む）。② `.claude/skills/hawkeye-task-cycle/SKILL.md` の §1 Intake に項目5の同要求を追加し、§5 完了報告に「完了条件に載せた運用コマンドの実出力を報告に含める（実行できない場合はunverified欄へ、誰が・いつ実行するかを添えて）」の項目を追加。③ T-006の完了条件を新書式と照合: 条件①②が運用コマンド `hawkeye case open HLIT --from-earnings --nav 10000` とその期待出力（`Guidance beat consensus +13.3%` を含む／`guidance_not_published` を含まない／理由欄が `(the source states none)`）で書かれており、テスト方法欄に実コマンド確認の実施時期（次回`/hawkeye-run`）も明記済み → **適合、書き換え不要**。④ `.venv\Scripts\python.exe -m pytest -q` → **850 passed**（失敗0、155秒）。commit: 16769f5（本体2ファイル）。**未検証**: 無し（ドキュメント変更のみで、本タスク自身の完了条件はすべてこの場で照合済み。ルールが実際に「テスト緑・経路壊れ」の再発を防ぐかは、次のタスク受付＝T-006の運用で観察される）。

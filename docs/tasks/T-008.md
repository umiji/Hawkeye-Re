# T-008: EWのニュース欄を決算理由の情報源に追加

## メタデータ

- 作成日: 2026-08-17
- 更新日: 2026-08-17
- 状態: 未着手
- 進捗: 0%
- 依存: 無し（T-003の情報源を足すタスクだが、T-003のコードは既に入っており着手の前提にならない）

## タスク詳細

（2026-08-17の初回実環境確認で判明した、T-003の情報源不足への対処）。**目的**: 決算がなぜその数字になったかを会社の言葉から読み取る工程（T-003で新設）が、実環境で**30件中0件**しか理由を取れなかった（2026-08-17実測、標本30）。原因は読み手の能力ではなく情報源で、現在この工程が読んでいるのは決算専門サイトEarningsWhispersのJSON API（`GET /api/epsdetails/{ticker}` の `summary` フィールド）1本のみ。この文章は「実績・アナリスト予想・囁き予想・前年比・会社紹介」の定型で構成されており、**過去四半期の理由を述べた文が構造的に含まれない**（会社の来期見通しは含まれるため、ガイダンス側は30件中7件取れている）。一方、同サイトのHTMLページ`https://www.earningswhispers.com/epsdetails/{ticker}` の `//*[@id="newscontent"]` にはFinancial results系の情報が別途掲載されている（User指摘、2026-08-17）。放置すると、EPSが大幅に上振れている銘柄について「一時的な要因（税効果・資産売却益・和解金）か、本業で稼いだ利益率か」が永久にUNVERIFIEDのままとなり、審理3役はその区別を付けられないままBUY/見送りを判定し、UserはPGYで実際に起きた『税効果だろう』という推測混じりの判断材料を受け取り続ける（金銭判断に直接影響）。また、ほぼ確実に0件と分かっている工程に毎回30エージェント分の費用を払い続けることになる（2026-08-17実測: セッション費用$180のうち相当部分がこの工程）。**変更範囲**: `hawkeye/marketdata/whispers.py`（`newscontent` 相当の内容を取得する経路の追加）／`hawkeye/scout/cause_agent.py`（`CauseRequest.summary` に渡す本文の組み立て。読み手のプロンプトと4つの検査は原則そのまま）／対応するテスト。**着手前に調査が必要な未確認事項（推測で埋めない）**: ① `#newscontent` を供給する経路が不明。2026-08-17に `/api/news/{ticker}` と `/api/newscontent/{ticker}` を実際に叩いたところ**どちらもHTTP 302**（本文0バイト）で、単純な兄弟エンドポイントでは取得できないことを確認済み。HTMLページ本体の取得と解析が必要か、別のAPIパスが存在するかは未調査。② HTMLを取得・解析する場合の利用規約・robots・レート制限の可否。③ 取得した本文で現在の `summary` を**置き換える**のか、**併記する**のか（併記すると引用の照合対象が2本になり、検査1「引用が原文に存在すること」の実装に影響する）。**禁止事項**: 読み手に渡す情報の範囲を広げるだけで、**読み手にサプライズ率（EPS・売上が何%上振れたか）を見せてはならない**（`cause_agent.py` 冒頭に記載のUser決定 2026-08-17。理由が存在するという前提を渡した時点で、原文に無い理由を作る失敗が再発する）。引用の照合・単位・期間の4検査は緩めない。ガイダンス読み取り（`guidance_agent.py`）の入力は変更しない。

## 完了条件

① 新しい情報源から本文を取得できることを、実銘柄1件で実コマンドにより確認する（例: AMBQで取得した本文に、`summary` には無い文が含まれていることを目視で示す）② 決算理由の読み取りを同じ30件に対して再実行し、`explained: true` の件数が0件から増えることを実測で示す（増えなかった場合は「この情報源にも理由は無い」という測定結果として `docs/knowledge/MEASUREMENTS.ja.md` に記録し、案として工程の停止をUserに提示する）③ 取得失敗・空応答のときに、理由を名前付きで記録し「読んだが無かった」と取り違えないことを確認するテストが成功する ④ `pytest` が全件成功する<br>**テスト方法**: 単体（オフライン。固定の応答文字列を使い、本文の組み立てと取得失敗時の名前付き理由を検証）／実環境確認は①②で必須。実施は次回以降の `/hawkeye-run` 実行時にUserの同席で行う。<br>**停止条件**: `CLAUDE.md` の共通停止条件に加え、調査の結果、利用規約・robots上取得が適切でないと判断される場合は実装に進まずUserに報告して停止する。

## 証拠

2026-08-17測定: EWサマリーからの理由抽出 0/30。`/api/news/`・`/api/newscontent/` はHTTP 302。

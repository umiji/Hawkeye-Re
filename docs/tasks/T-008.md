# T-008: EWのニュース欄を決算理由の情報源に追加

## メタデータ

- 作成日: 2026-08-17
- 更新日: 2026-08-18
- 状態: 実装完了・実環境検証待ち（読み手側の実測のみ残)
- 進捗: 90%（実装・テスト・完了条件①③④⑤⑥を達成。②は情報源側を実測済みだが、読み手側の `explained: true` 件数は次回 `/hawkeye-run` でUser同席のもと測定。無料枠上限のUser判断は2026-08-18に決着し、モデル移行はT-011へ引き継いだ）
- 依存: 無し（T-003の情報源を足すタスクだが、T-003のコードは既に入っており着手の前提にならない）

## タスク詳細

（2026-08-17の初回実環境確認で判明した、T-003の情報源不足への対処）。**目的**: 決算がなぜその数字になったかを会社の言葉から読み取る工程（T-003で新設）が、実環境で**30件中0件**しか理由を取れなかった（2026-08-17実測、標本30）。原因は読み手の能力ではなく情報源で、現在この工程が読んでいるのは決算専門サイトEarningsWhispersのJSON API（`GET /api/epsdetails/{ticker}` の `summary` フィールド）1本のみ。この文章は「実績・アナリスト予想・囁き予想・前年比・会社紹介」の定型で構成されており、**過去四半期の理由を述べた文が構造的に含まれない**（会社の来期見通しは含まれるため、ガイダンス側は30件中7件取れている）。一方、同サイトのHTMLページ`https://www.earningswhispers.com/epsdetails/{ticker}` の `//*[@id="newscontent"]` にはFinancial results系の情報が別途掲載されている（User指摘、2026-08-17）。放置すると、EPSが大幅に上振れている銘柄について「一時的な要因（税効果・資産売却益・和解金）か、本業で稼いだ利益率か」が永久にUNVERIFIEDのままとなり、審理3役はその区別を付けられないままBUY/見送りを判定し、UserはPGYで実際に起きた『税効果だろう』という推測混じりの判断材料を受け取り続ける（金銭判断に直接影響）。また、ほぼ確実に0件と分かっている工程に毎回30エージェント分の費用を払い続けることになる（2026-08-17実測: セッション費用$180のうち相当部分がこの工程）。**着手前調査の結果（2026-08-17完了。詳細と根拠は設計メモ `docs/design/CAUSE_SOURCE_EXTRACTION.ja.md`）**: ① 取得経路は判明し、**APIで取れるためスクレイピングは不要**。EWページのJavaScript（`/js/news.js`）を読み、`GET /api/epsdetails/{ticker}` の応答にある `fileName` 欄（現在は読まずに捨てている）を使って `GET /api/newsarticle/{ticker}/{fileName}` を叩くと、JSONの `article` 欄に決算プレスリリース全文が入ることを確認。同じ30銘柄で **30/30取得成功・30/30が決算発表日と一致・29/30が理由を述べる語を含む**。② robots.txtに禁止指定は無く、利用規約は個人の非商用利用を明示的に許可（再配布は禁止）。既に依存している `/api/epsdetails/` と同じ規約が同じくかかっており、新しい種類のリスクは持ち込まない。③ 現行 `summary` は**置換**する（併記の利得が無いため。根拠は設計メモ §3.4）。**変更範囲（2026-08-17に拡大。User決定により抜き出し工程を追加）**: `hawkeye/marketdata/whispers.py`（`fileName` の保持と `/api/newsarticle/` 取得経路の追加、HTML→テキスト変換）／`hawkeye/marketdata/gemini.py`（新規。プレスリリースから理由の書かれたブロックを**原文のまま抜き出す**要求。鍵は `.env.local` の `GEMINI_API_KEY`、429バックオフ必須）／`hawkeye/scout/cause_source.py`（新規。抜き出し結果を原文と機械照合し、検証済み抜粋を組み立てる）／`hawkeye/scout/cause_agent.py`（検査①の照合先を元のプレスリリース全文に据え置いたうえで、読み手への入力を検証済み抜粋にする。プロンプトと4検査の中身は原則そのまま）／対応するテスト。**禁止事項**: 抜き出し役に**要約・言い換えをさせてはならない**（13銘柄の実測で、明示的に禁止してもGeminiが1件、原文に無い文を合成した。機械照合が捕捉。要約方式ではこれが検査を素通りする）。**検査①の照合先を、抜粋ではなく元のプレスリリース全文から変えてはならない**。読み手に**サプライズ率（EPS・売上が何%上振れたか）を見せてはならない**（`cause_agent.py` 冒頭に記載のUser決定 2026-08-17。理由が存在するという前提を渡した時点で、原文に無い理由を作る失敗が再発する）。引用の照合・単位・期間の4検査は緩めない。ガイダンス読み取り（`guidance_agent.py`）の入力は変更しない。Geminiに送ってよいのは**会社が既に公開しているプレスリリース本文のみ**で、Hawkeyeの判断ロジック・保有情報・台帳の内容を送ってはならない。

## 完了条件

① 新しい情報源から本文を取得できることを、実銘柄1件で実コマンドにより確認する（例: AMBQで取得した本文に、`summary` には無い文が含まれていることを目視で示す）② 決算理由の読み取りを同じ30件に対して再実行し、`explained: true` の件数が0件から増えることを実測で示す（増えなかった場合は「この情報源にも理由は無い」という測定結果として `docs/knowledge/MEASUREMENTS.ja.md` に記録し、案として工程の停止をUserに提示する）③ 取得失敗・空応答のときに、理由を名前付きで記録し「読んだが無かった」と取り違えないことを確認するテストが成功する ④ **抜き出し役が原文に無い文を返したとき、機械照合がそれを捨て、捨てた事実を名前付きで記録することを確認するテストが成功する**（2026-08-17の実測でAIIに実際に発生した合成文を固定の応答として使う）⑤ **検査①の照合先が抜粋ではなく元のプレスリリース全文であることを確認するテストが成功する**（抜粋に含まれない原文中の文を引用しても通る／原文のどこにも無い文は通らない）⑥ `pytest` が全件成功する<br>**実行コマンド（Userが打つ）**: `.venv/Scripts/hawkeye.exe cause source AMBQ` を実行し、①ベンダー要約より決算発表文が桁違いに長いこと ②読み手に渡す抜粋に、要約には存在しない理由の文（例: `driven by strong demand for edge AI solutions`）が含まれること ③抜き出し役が原文に無いブロックを返した場合、それが「却下したブロック」として画面に出ること、を確認する。<br>（当初この欄には `hawkeye case open AMBQ --from-earnings` と書いていたが、実装中に、名指し銘柄の案件生成（`judge_ticker`）は決算理由の読み取りを一切キューに積まない経路であることが判明したため、この変更を実際に見せられるコマンドに差し替えた。2026-08-17）<br>**テスト方法**: 単体（オフライン。固定の応答文字列を使い、本文の組み立て・機械照合・取得失敗時の名前付き理由を検証。Geminiへの実通信はテストでは行わない）／実環境確認は①②と上記の実行コマンドで必須。実施は次回以降の `/hawkeye-run` 実行時にUserの同席で行う。<br>**停止条件**: `CLAUDE.md` の共通停止条件に加え、①調査の結果、利用規約・robots上取得が適切でないと判断される場合（→ 2026-08-17に調査完了、問題なしと判断）②30銘柄実測で `explained: true` が0件のままだった場合は、実装を広げずMEASUREMENTSに記録してUserに報告し停止する。

## 証拠

2026-08-17測定: EWサマリーからの理由抽出 0/30。`/api/news/`・`/api/newscontent/` はHTTP 302。

2026-08-17 着手前調査（設計メモ `docs/design/CAUSE_SOURCE_EXTRACTION.ja.md` に全文）:

- 取得経路: `/api/epsdetails/{ticker}` の `fileName` → `/api/newsarticle/{ticker}/{fileName}`。同じ30銘柄で **30/30取得成功・30/30が決算発表日と一致・29/30が理由を述べる語を含む**。本文は1銘柄あたり中央値 約25,900文字（現行サマリーの約45倍）。
- 抜き出し（Gemini `gemini-2.5-flash`、温度0・思考予算0・構造化出力）を13銘柄で実測: **81ブロック中79が原文照合を通過**。抜粋後の分量は原文の0〜14%（実測 90〜2,932文字）。
- **却下2件の内訳がこの設計の中核根拠**: AIIは「原文のままコピーせよ」と明示指示したにもかかわらずGeminiが会社の文の**金額を1桁書き換えた**本物の書き換え。DFDVは丸引用符と直引用符の差だけの偽陽性で、照合前の文字正規化が必要と判明。
  - **【2026-08-18 訂正 / T-013】** この行は当初「Geminiが新しい1文を合成した捏造（220文字中、原文と一致する最長連続部分は25文字）」と書いていたが、**誤りだった**。発表文（38,103文字）を全文検索すると当該の文は実在し、違いは `$75 million` → `$65 million` の1文字だけ、最長一致は**210文字**である。当時は発表文冒頭の別の箇条書きと突き合わせており、本文を検索していなかった。詳細は `docs/design/CAUSE_SOURCE_EXTRACTION.ja.md` §3.3。
- レート制限: 5リクエスト連投で HTTP 429。`retryDelay` に従うバックオフを入れれば連続成功。1リクエスト 1.6〜4.9秒。**（この読みは実装後の30銘柄実測で誤りと判明。下記参照）**
- 利用規約 `https://www.earningswhispers.com/usage`（2023-07-18更新）は個人の非商用利用を明示的に許可、再配布は禁止。robots.txt に禁止指定なし。

2026-08-17 実装後の実測（30銘柄、本番コード経路 `ReleaseCauseSource` を使用）:

- **完了条件①（達成）**: `.venv/Scripts/hawkeye.exe cause source AMBQ` の実出力 —— ベンダー要約529文字／決算発表文21,874文字／読み手に渡す抜粋928文字（発表文の4%）。抜粋には要約に存在しない `driven by strong demand for edge AI solutions` が含まれる。
- **完了条件②（情報源側のみ達成。読み手側は次回 `/hawkeye-run` でUser同席のもと測定）**: 同じ30銘柄で **22件が抜粋を生成**、1件（DFDV）が `no_cause_in_release`、7件が無料枠上限による `extractor_call_failed`。分量は発表文738,672文字 → 抜粋33,832文字（4.6%）で、現行要約17,606文字の約2倍に収まる。
- **完了条件③④⑤⑥（達成）**: `.venv/Scripts/python.exe -m pytest -q` → **894 passed**（着手前853 + 新規41）。
- **却下ブロックの内訳（設計の中核根拠が実データで裏付いた）**: 原文照合に落ちた3件を1件ずつ原文と突き合わせた結果、**本物の書き換えはHQIの1件のみ**（`year-on-year` ↔ 原文 `year-over-year`）。HLIT（文末の句読点）とSGA（先頭の大文字化）は照合が厳しすぎたための誤判定と判明したため、大文字小文字の無視と両端句読点での再照合を追加し、さらに**採用ブロックを原文の文字で切り出す**方式に変更した（抜粋が常に原文の部分文字列になり、後段の引用照合が構造上必ず通る）。修正後の再実行で HLIT=採用 / SGA=採用 / HQI=却下 を確認。
- **⚠️ 無料枠は1スキャンに足りない**: `gemini-2.5-flash` の無料枠は **1日20リクエスト**（429本文が `limit: 20`、`quotaId: GenerateRequestsPerDayPerProjectPerModel-FreeTier`、`quotaValue: "20"` と明示）。1スキャン30銘柄なので完走できない。対応案A（有料枠。概算で1スキャン日あたり数円〜十数円、かつ送信内容が学習に使われない）／B（別モデルの無料枠）／C（対象銘柄を絞る）は設計メモ §3.5 に整理。

**2026-08-18 追記（T-011へ引き継ぎ、User判断は解消済み）**

- 上の20/日は**正しい数字**である。2026-08-18に再測定しても同じ `HTTP 429` が返り、Userが確認したモデル別上限表とも一致する。日付が変わっても枠は戻らない。
- **User判断は決着した: 案B（別モデルの無料枠）を採用**し、`gemini-3.5-flash-lite`（日次500・分間15）へ移行する。実装・検証は **T-011**（`docs/tasks/T-011.md`）で行う。この上限問題を理由にT-008を保留する必要はもう無い。
- ただし本タスクの**完了条件②の読み手側（`explained: true` の件数）は依然として未測定**であり、次回 `/hawkeye-run` でUser同席のもと測る点は変わらない。T-008が「実環境検証待ち」である理由はこちら1点に絞られた。
- なお、この記録には**未解決の矛盾**がある。上の完了条件②は30銘柄で22件の抜粋生成（＝22回以上の成功）を記録しており、同日に13銘柄の測定も行っている。合計35回以上は日次20件と噛み合わない。原因は未特定で、推測で埋めないこと（詳細は `docs/knowledge/MEASUREMENTS.ja.md`）。

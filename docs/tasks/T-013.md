# T-013: 却下ブロックを近似一致で救い件数をレポートに出す

## メタデータ

- 作成日: 2026-08-18
- 更新日: 2026-08-18
- 状態: 完了
- 進捗: 100%（設計ノートの提出は 2026-08-18 にUserの判断で省略された。「設計ノート書かなくていい。実装を先に進めてくれ」との明示指示による。統治ルールを満たしていない点はここに明記して残す）
- 依存: T-012（ブロッカー、解消済み: 本タスクが救おうとする「却下ブロック」は、T-012の修正で発生源の大半が消えた。T-012の前に作っていれば、こちらのテキスト変換の欠陥を静かに埋めてしまい原因に到達できなかった。順序が逆であってはならなかった）

## 目的

毎日の走査は、決算を出した約30銘柄について「なぜこの四半期はこの数字になったのか」を会社自身の決算発表文（約25,900文字）から抜き出す。抜き出しは安価なモデル（`gemini-3.5-flash-lite`）に「該当箇所を一字一句そのまま写せ」と頼み、返ってきたブロック1件ごとに「本当に発表文の中にその文字列が存在するか」をコードで照合してから審理3役（Bull / Adversary / Judge）に渡す。この照合だけが、会社が言っていない文が会社の発言として審理に渡るのを防いでいる。

**放置した場合に困ることは2つある。**

**1つ目: 照合に落ちたブロックがあっても、Userにも審理3役にも何も伝わらない。** 落ちたブロックはその場で捨てられ、抜粋はその分だけ薄くなる。だが抜粋が薄い理由が「会社が多くを語らなかった」のか「却下されて消えた」のかは、走査レポートを見ても区別が付かない。2026-08-18のT-012では、5銘柄・13ブロック・3,320文字の**実際の会社の説明**が捨てられていたが、それが表に出たのは測定用スクリプトを書いたからであって、通常の運用では永久に見えなかった。**審理3役は、消えたことを知らないまま少ない材料で買う・買わないを述べる。**

**2つ目: 厳密一致を1文字でも外すと即座に捨てるため、救えるものまで捨てている。** T-012で判明したとおり、却下13件の実体は「モデルの捏造」ではなく「こちらのHTML→文章変換が句読点の前に空白を入れていた」ことだった。T-012はその発生源を直したが、**同種のずれ（typo、改行の入り方、記号の揺れ）は今後も起こりうる**。厳密一致だけでは、原文に存在するのに1文字違うだけで会社の説明を失い続ける。

**なぜ「救う」ことが安全なのか。** 救済とは、モデルが書いた文字を通すことではない。**モデルの文字は常に捨て、発表文の中から最も近い箇所を探し、その箇所の発表文の文字を切り出して抜粋に載せる。** 抜粋は今までどおり発表文の部分文字列であり続けるので、審理3役が読むのは常に会社の言葉である。この「原文の文字を切り出す」処理自体はT-008で既に実装済みで、本タスクはそこへ近似一致の結果を流し込む経路を足すだけである。

## 変更範囲

- `hawkeye/scout/cause_source.py` — `build_cause_text` に近似一致の段を追加する。厳密一致（＋両端句読点の1回再試行）が外れた場合に限り、発表文の中から最も近い箇所を探し、見つかれば**発表文の文字**を切り出して採用する。閾値未満は従来どおり却下。救済した件数を `CauseText` に持たせる。
- `hawkeye/scout/scout.py` — 走査が却下件数と修正件数を決算実績の行（`earnings_prints`）に運ぶ経路。
- `hawkeye/ledger/` — 上記2つの整数を保存する列（**ブロック本文は保存しない**）。
- `hawkeye/reports/render_ja.py` および走査レポートを組み立てる箇所 — 銘柄ごとに「却下N件 / 修正N件」を出す。**0件のときも明記する**（出さないと機能が動いていない状態と区別が付かない）。
- `tests/test_cause_release_source.py` / `tests/test_cause_source.py` — 近似一致の単体テスト。
- `docs/knowledge/MEASUREMENTS.ja.md` — 閾値の選定根拠（実測）。
- `docs/design/CAUSE_SOURCE_EXTRACTION.ja.md` — §4.6の続きとして本変更を反映。
- `docs/task-list-hawkeye-re.csv` / 本ファイル。

## 禁止事項

- **モデルが書いた文字を抜粋に載せない。** 近似一致で当たっても、抜粋に入るのは必ず発表文から切り出した文字とする。この一線を越えると、T-008以来の「抜粋は発表文の部分文字列である」という保証が崩れ、下流の引用チェックが構造的に通るという前提も同時に崩れる。
- **閾値を推測で決めない。** 対照群（別会社の発表文／同じ発表文の別の箇所）とT-012で記録済みの却下13ブロックを標本として実測で決め、根拠を残す。「0.85が業界標準だから」は根拠にしない。
- **却下ブロックの本文を台帳に保存しない。** Userが不要と明示（2026-08-18）。必要なときは発表文を取り直して `hawkeye cause source ティッカー` で再現できる。
- **Agentに判定させない。** 機械的処理で足りることをAgentに任せると、APIモードとセッションモードで結果が変わり、両エンジンの比較可能性（`CLAUDE.md` invariant 4）が壊れる。
- **審理3役に渡す抜粋の中身以外を変えない。** 順位付けの点数計算には理由が一切入っていないことを2026-08-18に確認済み（`score_candidate` の入力はEPSサプライズ・売上サプライズ・決算当日ギャップの3つのみ）。本タスクで点数に手を入れない。

## 完了条件

① **運用コマンド**: `.venv/Scripts/hawkeye.exe report scan` の出力に、銘柄ごとの「却下N件 / 修正N件」が現れること。却下0件・修正0件の銘柄についても明記されていること。実出力を証拠欄に貼る。

② **閾値の実測による決定**: 実測で閾値を選び、選定表を証拠欄に残す。

   **【2026-08-18 改訂】** 当初この条件は「AIIの捏造文を却下しつつT-012の13ブロックを
   救う閾値」を求めていた。実測の結果、**その2つを分ける閾値は存在しない**（AII 0.9955 が
   救うべきSDRLの3件（最小0.9932）より上に来る）。さらに、AIIを「捏造文」とした記録
   そのものが誤りだった（実際は原文に実在する文の金額1桁の改変。最長一致210/220文字）。
   Userの判断（「なんでAIIにばかりFocusしているのか？ 今後の数多くの銘柄選定に際して
   最も効果的な方法を考えて実装してくれ」）により、条件を次に置き換えた:

   - **閾値は「同じ箇所か、別の箇所か」を分けるものとして、対照群から実測で決める。**
     (a) 別会社の発表文に対する類似度の上限、(b) 同じ発表文の中の別の箇所に対する
     類似度の上限、(c) 救うべき本物（T-012の13ブロック）の下限 —— (a)(b) を上回り
     (c) を下回る値を採る。窓が存在しない場合は実装せず停止して報告する。
   - **「こちら側の変換不具合」と「抜き出し役による語句の改変」は、類似度ではなく
     差分の中身（英数字が違うか）で分け、別々に数えて表示する。**

③ **回帰**: `.venv\Scripts\python.exe -m pytest -q` が全件成功（**907件以上**、失敗0）。

④ **30銘柄の実測**（T-011/T-012と同一の30銘柄）: 救済によって新たに採用されたブロックが1件でもあれば、**その全文を発表文と目視で突き合わせ、会社が実際に書いた文であることを1件ずつ確認する**。T-012直後の実測は却下0/160なので、救済0件（＝働く場面が無い）という結果も正常であり、その場合は「0件だった」ことを証拠に記す。

⑤ **単体テスト**: 発表文に1文字の差で存在するブロックが救われ、**採用された文字が発表文のものであること**（モデルが送ってきた文字ではないこと）を直接検証する。AII型（大半が一致しない）が却下されることも検証する。

## テスト方法

- **単体（オフライン）**: 完了条件⑤。`build_cause_text` に発表文と偽のブロック列を直接与える。ネットワーク不要。
- **回帰**: `pytest` 全件（完了条件③）。
- **閾値の実測**: 完了条件②。T-012の却下13ブロックとAIIの捏造文を固定入力として使う。**Geminiへの新規リクエストは不要**（記録済みデータで足りる）。
- **実環境確認**: 完了条件①④。Claudeが実行し、出力をUserに提示する。④はGeminiに30リクエスト（日次500に対して十分小さい）。

## 停止条件

`CLAUDE.md` の共通停止条件に加えて、本タスク固有:

- **完了条件②で、対照群を上回り本物を下回る閾値が見つからない場合は実装せず停止して報告する。** 片方を諦めて通すことは禁止。（当初の「AIIを却下しつつ13件を救う」版はこの停止条件に該当し、実際に停止して報告した。上記②の改訂はその報告に対するUserの判断による）
- **救済されたブロックの中に、会社が書いていない文が1件でも混じっていた場合は即座に停止する。** その時点で本機能は目的を果たしていない。
- **設計ノート（またはMASTER_OVERVIEW §4/§5の更新）へのUser承認前に実装を始めない**（`CLAUDE.md` 統治ルール）。

## 背景（2026-08-18のUserとの合意経緯）

このタスクは当初「捏造アラートを通常の走査でも立てられるか」という問いから始まり、対話の中で範囲が2度変わった。**最終的に合意した形と、そこから外した案の理由を残す** —— 外した案はどれも一見もっともらしく、記録が無ければ再発見される。

**外した案1: 却下ブロックの本文を台帳に保存する。** 当初は「捏造率を時系列で測れる」ことを理由に推奨したが、Userが不要と判断（「邪魔だし」）。発表文はいつでも取り直せ、`hawkeye cause source` で同じ情報が再現できるため、保存は重複である。**残すのは整数2つだけ。**

**外した案2: Agentが原文を読んで最終判断する。** 機械的処理で足りるため不要。加えて、Agentに判定させるとAPIモードとセッションモードで結果が変わる。

**外した案3: 何もしない。** 危険なものは既に審理に届いておらず、T-012直後の実測は却下0/160。「今日の問題を解く機能ではない」という理由で見送りも支持できたが、Userは修正機能と件数表示を作る判断をした。

**設計上のトレードオフ（合意のうえで受け入れたもの）**: 救済機能は**こちら側の変換バグを静かに埋める**。T-012の欠陥が見つかったのは、照合が13件を騒がしく捨てていたからで、救済が先にあれば静かに救われて原因に到達できなかった。**だからこそ修正件数の表示が必須**であり、機能の片割れではなくセットで意味を持つ。「修正3件」が出ていれば、抜粋は正しく届きつつ、Userは「またこちら側で何か壊れている」と気づける。

**調査記録**: 近似一致の手法と閾値の出どころは `docs/tasks/T-012.md` の「調査記録」にある（Google の LangExtract は `difflib.SequenceMatcher` で類似度0.85）。T-012では「照合を緩めることになる」として採らなかったが、**本タスクが採るのは別の機構である** —— 照合そのものは緩めず、外れたときに**発表文の文字を切り出して置き換える**。モデルの文字は通らないので、T-012の禁止事項とは衝突しない。この区別はT-012の時点では明確に述べられておらず、2026-08-18の対話で整理された。

## 証拠

すべて 2026-08-18 実測。コマンドと出力は本文中にそのまま貼る。

### ① 走査レポートに件数が出ること

本番の台帳には触れず、使い捨ての作業領域（`HAWKEYE_VAR`）で実際に
`hawkeye scout --days 4` → `hawkeye rank` → `hawkeye report scan` を通した実出力:

```
## ⑤ 決算発表文の読み取り

5銘柄の決算発表文から、審理に渡す抜粋を作りました。抜粋に入った文の数は **28件** です。

- そのまま採用: **28件**
- こちらの文章変換のミスを直して採用: **0件**
- 抜き出し役が会社の語句を改変していたので原文の文字に置き換えて採用: **0件**
- 発表文のどこにも近い箇所が無く却下: **0件**
```

同コマンドが出力するCSVは全銘柄分を持つ（ゲート通過0件の日でも銘柄ごとに出る）:

```
ティッカー | 発表文 採用ブロック数 | 発表文 変換ミスを修正した数 | 発表文 抜き出し役が改変した数 | 発表文 却下した数
TOMZ | 11 | 0 | 0 | 0
LTRN | 3  | 0 | 0 | 0
FLXS | 4  | 0 | 0 | 0
CRGO | 8  | 0 | 0 | 0
HURA | 2  | 0 | 0 | 0
```

審理に送る銘柄ごとの行（§②）は `_cause_source_line` が出す。この実走査ではゲートを
通過した銘柄が0件だったため §② には銘柄が並ばず、テストで確認した（下記③）。
**§⑤ を足したのはこのためである** —— ゲート通過0件の日こそ、こちら側の不具合が
誰にも見られずに走る日である。

### ② 閾値の実測（0.95）

| 母集団 | 件数 | 最大 |
|---|---|---|
| 別会社の発表文に対する類似度 | 356組 | **0.7018** |
| 同じ発表文の別の箇所（本来の箇所を隠して再照合） | 86件 | **0.8951** |
| T-012で却下されていた13ブロック（救うべき本物） | 13件 | 最小 **0.9932** |

窓 (0.8951, 0.9932] → 採用 **0.95**。ALCOの「3か月 vs 9か月」の取り違えは 0.7636 止まり。

**類似度では2種類の救済を分けられないことも実測した** —— AII（数字1桁の改変）は
0.9955 で、救うべきSDRLの3件より上に来る。差分に英数字が含まれるかで分けると、
13件すべて「含まない」／AIIのみ「含む」で完全に分離する。全表は
`docs/knowledge/MEASUREMENTS.ja.md`。

### ③ 回帰テスト

```
.venv\Scripts\python.exe -m pytest -q
925 passed in 41.01s
```

セッション開始時 907 → **925**（+18）。内訳: 近似一致の単体テスト（`test_cause_source.py`）、
走査から台帳・レポートまでの配線（`test_scout_quality_wiring.py`）、保留中の走査ファイルの
往復（`test_scan_store.py`）、レポート表示（`test_scan_report.py`）。

### ④ 30銘柄の実測（T-011/T-012と同一銘柄、本番経路）

```
excerpts built : 29 / 30
blocks returned: 160   kept 160  repaired 0  altered 0  refused 0
excerpt total  : 32591 chars of 737482
--- every rescued block, in full (0) ---
```

**救済0件。** T-012で発生源が消えているため、この機構が今日働く場面は無い。完了条件④が
「救済0件も正常な結果」と定めていたとおりで、目視で突き合わせるべき新規採用ブロックは
1件も出なかった。

記録済みの14ブロック（T-012の13件 + AII）を修正**前**の発表文に対して再生した結果:

```
ticker  blocks  kept  repair  alter  refuse
AII          1     1       0      1       0
ALCO         2     2       2      0       0
FOSL         1     1       1      0       0
MSGE         7     7       7      0       0
SDRL         3     3       3      0       0
totals: {'kept': 14, 'repaired': 13, 'altered': 1, 'refused': 0}
excerpt-is-a-substring failures: 0
```

AIIの改変内容（審理に渡るのは下段の原文の文字）:

```
extractor sent : ...reducing its aggregate retention from $95 million to $65 million
release says   : ...reducing its aggregate retention from $95 million to $75 million
```

### ⑤ 単体テスト（原文の文字が採用されること）

`tests/test_cause_source.py`:
- `test_a_changed_figure_is_rescued_in_the_release_s_own_digits` — 抜粋に `$327 million`
  が入り `$427` は入らないことを直接検証（＝モデルの文字ではなく発表文の文字）。
- `test_a_block_our_conversion_broke_is_repaired_not_refused` — T-012型（空白のみの差）が
  「修正」に計上されること。
- `test_a_block_with_a_substituted_word_is_carried_in_the_release_s_word` — 単語の置換が
  「改変」に計上され、抜粋には原文の語が入ること。
- `test_a_block_resembling_no_passage_is_refused` — 発表文のどこにも無いブロックは却下。
- `test_a_rescued_block_is_still_a_literal_substring_of_the_release` — 救済経路でも
  「抜粋は発表文の部分文字列」が保たれること。

### この作業中に判明した記録の誤り（独立に訂正済み）

閾値の実測中に、**設計の中核根拠として6か所に書かれていた「AII＝Geminiが文を合成した
捏造。220文字中の最長一致は25文字」という記述が事実と異なる**ことが判明した。実際は
原文に実在する文の金額1桁の改変で、最長一致は210文字、`$65 million` は発表文のどこにも
存在しない。当時の調査が発表文冒頭の別の箇条書きと突き合わせ、残る38,000文字を検索して
いなかったことが原因である。訂正した箇所:

- `hawkeye/scout/cause_source.py` モジュール冒頭
- `tests/test_cause_source.py` 冒頭とテスト名
- `docs/design/CAUSE_SOURCE_EXTRACTION.ja.md` §3.3
- `docs/tasks/T-008.md` / `docs/tasks/T-012.md`
- `docs/knowledge/MEASUREMENTS.ja.md`

**設計への含意**: この工程が守っているのは「モデルが文を捏造すること」ではなく
**「モデルが実在する文の中の数字を書き換えること」**である。後者のほうが発見が難しく、
審理3役が推論に使う数字を直接狂わせる。T-013の「改変」件数の表示は、この本当の失敗形態を
可視化するためのものである。

### 未検証

- 走査レポート §② の銘柄ごとの行を、**ゲートを通過した実銘柄で**表示した実例。今回の
  実走査はゲート通過0件だったため、テストでのみ確認している。次に候補が出る日の
  `/hawkeye-run` でUserが目にする。
- 救済が実際に働く場面（修正・改変が1件以上出る走査）。本番では30銘柄・実走査5銘柄とも
  0件だった。
- 統治ルール（設計ノートのUser承認）は今回スキップした（Userの明示指示）。

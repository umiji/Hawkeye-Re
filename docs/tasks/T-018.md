# T-018: 走査CSVにガイダンス6列を追加し、エラー列と比較の但し書きを直す

## メタデータ

- 作成日: 2026-08-19
- 更新日: 2026-08-19
- 状態: 実装完了・実環境検証待ち
- 進捗: 80%
- 依存: T-020（ブロッカー）

**依存の理由**: T-020 で「1件の決算が複数期の見通しを持てる」形に変わる。先に
この列を作ると、1期しか入らない前提の列を作った直後に作り直すことになる。
追加する6列のうち「対象期」は、まさにその複数期を表現する列である。

## 目的

### このファイルが最終的に何のためにあるか

`hawkeye report scan` は、その回の走査で何を見て何を審理に送り、他をなぜ落と
したかの報告を画面に出すと同時に、**全銘柄の表をCSVに書き出す**。画面の表は
読みやすさのために列と行を絞っているが、**CSVのほうは絞っていない**。

このファイルは、**Userが「今日の走査は本当に正しく動いていたか」を自分の目と
表計算ソフトで確かめるための唯一の全量データ**である。順位や採否を機械が
決めた根拠を、機械の言い分ではなく元の数字で検算するために存在する。

### 何が足りないか（その1: ガイダンスの実数値が無い）

CSVには「ガイダンス」という列が1つあるだけで、中身は「開示あり／開示なし」と
いう状態だけである（`_CSV_COLUMNS`、`hawkeye/reports/scan_report_ja.py:100`）。

```
| ガイダンス |
| 開示あり   |
```

EPS・売上については「実績/予想」と「サプライズ率」が全部入っているのに、
**見通しについては、会社が何と言ったのか・市場が何を期待していたのか・どちらが
上だったのかが、CSVから一切分からない。** 点数の内訳列（「ガイダンスで得た点」）
に結果だけが入るが、その点数がどの数字から出たのかは検算できない。

### 何が足りないか（その2: エラー列が3語しかない）

エラー列に入りうる値は次の3つだけである
（`hawkeye/reports/scan_report_ja.py:710-712`）。

| 表示 | 実際の意味 |
|---|---|
| 数値 | 決算専門サイト(EW)からEPS・売上の実績/予想が取れず、決算カレンダー(Finnhub)の数字で判定した |
| 見通し | 会社は見通しを述べているのに、こちらが読み取れなかった |
| 発表文 | 審理3役に渡す抜粋の一部が、却下されたか語句を改変された |

**「数値」という2文字から、上の説明にたどり着ける読み手はいない。** 2026-08-19
の走査でも EVGN と PONY の2行に「数値」とだけ出ており、Userから「これは何か」
という質問が実際に出た。

### 何が足りないか（その3: 比較の根拠として嘘の但し書きが出る）

会社見通しとコンセンサスを比べた結果には、「何と何を比べたか」を説明する
但し書きが添えられる。その文言は2つある
（`hawkeye/reports/quality_ja.py:145-146`）。

```python
"on_eps": "EPSレンジの中央値で比較",
"on_revenue": "売上レンジの中央値で比較(EPSレンジの開示が無いため)",
```

**括弧内の「EPSレンジの開示が無いため」は、EPSレンジが開示されていても付く。**
比較処理（`_guidance_leg`、`hawkeye/scout/quality.py`）は、EPSと売上の**両方**で
比べられたときに `on_eps` と `on_revenue` の両方を立てる仕様であり、そのとき
画面には矛盾した2行が並ぶ。

2026-08-19 の走査で AS に実際にこう出た。

```
  ガイダンス: ほぼ一致 -17.9%
    - EPSレンジの中央値で比較
    - 売上レンジの中央値で比較(EPSレンジの開示が無いため)
```

AS は EPS レンジ($0.31〜0.33)を開示している。1行目でEPSで比較したと言った
直後に、2行目でEPSの開示が無いと言っている。

### 放置した場合に困ること

- **検算ができない。** 見通しの点数がおかしいと感じても、CSVを見て確かめる
  手段が無く、台帳を直接SQLで開くしかない。それはUserの作業ではない。
- **T-020 で見つかったような取りこぼしを、Userが自力で発見できない。** AS の
  複数期ガイダンスの件は、発表文を人が読んで初めて分かった。会社見通しと
  コンセンサスが対象期つきでCSVに並んでいれば、表を眺めるだけで気づける。
- **エラー列が読めないため、その行を無視するか調べ直すかの判断がつかない。**
  「数値」は「決算カレンダーの数字で代用した（＝順位は付いているが出所が違う）」
  という意味であって、失敗を意味しない。今の表示はそれを伝えていない。

## 変更範囲

- `hawkeye/reports/scan_report_ja.py` の `_CSV_COLUMNS` と `_csv_cell` —
  ガイダンス6列の追加と、エラー列の文章化。
  - 追加する6列: EPSガイダンス（対象期／会社見通し／コンセンサス）、
    売上ガイダンス（対象期／会社見通し／コンセンサス）。
  - **対象期は1列に併記する**（User決定、2026-08-19）。書式は
    `2026-Q3（翌四半期）` / `FY2026（通期）`。生の期表記と分類を同じセルに入れる。
  - **エラー列は1文の説明文にする**（User決定、2026-08-19）。例:
    「決算専門サイトからEPSの数値が取れず、決算カレンダーの数字で判定しました」。
    複数該当する場合の連結方法は実装時に決め、理由を証拠欄に書く。
  - **会社見通しがレンジのときは、中央値だけをCSVに入れる**（User決定、
    2026-08-19）。$0.31〜0.33 なら `0.32`。表計算でそのまま割り算できることを
    優先する。レンジの上下は画面の表と台帳に残るため、失われはしない。
- `hawkeye/reports/quality_ja.py` — 比較の但し書き `on_revenue` の文言。
  「EPSレンジの開示が無いため」を無条件に付けるのをやめる。EPSでも比較できた
  ときと、売上でしか比較できなかったときとで、別の文言が出るようにする。
  どちらの状態かは既存のフラグ（`on_eps` が立っているか）で判別できるため、
  比較処理側を変える必要はない。
- `tests/` — 上記の単体テスト。

## 禁止事項

- **画面の表（`hawkeye report scan` が印字する Markdown 表）に列を足さない。**
  画面は読みやすさのために絞られており、それは意図された設計である。列の追加は
  CSVだけに行う。（但し書きの文言修正は別で、下の項を参照）
- **比較の但し書きは、文言だけを直す。** どちらの但し書きを出すかを決めている
  比較処理（`hawkeye/scout/quality.py` の `_guidance_leg`）のロジック、および
  `on_eps` / `on_revenue` というフラグの立ち方は変えない。壊れているのは
  日本語の文であって、判定ではない。
- **既存の列を削除・改名・並べ替えしない。** 追加と、エラー列の値の書き換え
  だけを行う。
- **CSVのファイル名に触らない。** それは T-019 の範囲。
- **既に別列にある詳細な理由（「取得できなかった理由」「見通しが読めなかった
  理由」）を消さない。** エラー列は要約であり、詳細列との重複は許容する。
- **点数・順位・ゲート判定のロジックに触らない。** これは表示だけのタスクである。
- **CSVの文字コード（BOM付きUTF-8）を変えない。** 日本語WindowsのExcelが
  文字化けするのを防ぐために付いている。
- **仕様が不明な点を推測で埋めない。**

## 完了条件

**2026-08-19 改訂（着手時の実データ調査による）。** 旧版は①〜⑥のすべてを
「走査番号3に対して `hawkeye report scan --scan-id 3` を実行して確認する」と
書いていたが、着手時に台帳を開いて確認した結果、次の2点が事実と食い違って
いたため改訂した。改訂の根拠は「証拠」欄に実データで残す。

- 走査の記録（`screened_candidates` テーブル）には、会社の見通しとコンセンサス
  の実数値が1つも保存されていない。保存されているのは「開示あり／開示なし」と
  いう状態と、読めなかった理由の2つだけ。数値は決算行（`earnings_prints`）と
  コンセンサス行（`consensus_snapshots`）にあるが、走査レポートはそれらを読ま
  ない。**User決定（2026-08-19）により案Aを採る**——走査時に数値を走査の記録へ
  焼き付ける。よって走査番号3の行には項目自体が存在せず、6列は空になる。
- `hawkeye report scan` は比較の但し書きを1行も印字しない。印字しているのは
  `hawkeye rank`（走査で順位を付ける）・`hawkeye case open`（審理を開始する）・
  `hawkeye guidance submit`（読み取った見通しを台帳に入れる）・
  `hawkeye cause submit` の4つ。

① CSVのヘッダ行にガイダンス6列が想定の見出しで存在することを、
   `hawkeye report scan --scan-id 3` が書き出した実ファイルの1行目で示す
   （**走査番号3で実施可能**）
② 見通しを開示した銘柄の行で、6列に実数値が入っていることを実ファイルの該当行
   で示す。対象期は `2026-Q3（翌四半期）` の形式であること。**複数期を述べた銘柄
   では両期が並ぶこと。** **実運用の走査1回での確認とする**——走査番号3の記録には
   項目が無いため。T-014 / T-015 / T-016 / T-020 が待っているのと同じ1回で確認
   する。実施者は User（走査の実行者）、時期はその走査の実行時

   **AS そのものでは確認できない（2026-08-19 追記）。** 走査は同じ決算を二度拾わ
   ない（重複排除）ので、AS の 2026-Q2 決算は次の走査には出てこない。AS で両期が
   並ぶのは AS が次の決算を出したときである。よって②の複数期の確認は、その走査に
   出た「複数期を述べた任意の銘柄」で行う。AS の決算行が2期を持つこと自体は実台帳で
   確認済み（証拠欄「実台帳への書き込み」）。

   **②が閉じるまで T-018 全体を保留する（User決定 案2、2026-08-19。）** ②-a
   （開示ありの銘柄で6列が埋まる）だけを先に閉じる案1は採らない。複数期を述べる
   会社は、見通しを述べた会社のうち 23.8%（21件中5件、2026-08-19 実測）なので、
   見通しを述べた銘柄が数件しか出ない走査では複数期の銘柄が1つも含まれない可能性
   のほうが高い。**何回目の走査で閉じるかは読めず、複数回持ち越す前提である。**
③ 見通しを開示していない銘柄（RNW / PONY）の行で、6列が空になっており、`-` とも
   「開示なし」とも混同されないことを示す。**②と同じ走査での確認とする**——走査
   番号3では全銘柄が空になり、開示ありの銘柄との差が出ないため検証にならない
④ 2026-08-19 の走査で「数値」と出ていた2行（EVGN / PONY）のエラー列が、読んで
   意味の分かる1文になっていることを実ファイルの該当行で示す
   （**走査番号3で実施可能**）
⑤ エラーが1つも無い行（AS / RNW / KLAR）では、エラー列が空のままであることを
   実ファイルの該当行で示す（**走査番号3で実施可能**）
⑥ EPSと売上の両方で比較できた銘柄（AS）について、「EPSレンジの開示が無いため」
   が**出ない**ことを実出力で示す。確認コマンドは `hawkeye guidance submit`
   ——実台帳の AS に通期の見通しを追加する操作そのものであり、`/hawkeye-run` の
   手順に含まれる。併せて、売上でしか比較できなかった銘柄（KLAR）で、その理由が
   読める但し書きが**出る**ことも実出力で示す（片方だけでは、常に消すだけの実装
   でも通ってしまう）
⑦ `.venv\Scripts\python.exe -m pytest -q` が全件成功する（着手前 987件＋新規分）

①④⑤⑦は本タスクの実装者が実施する。②③は実運用の走査を待つ。⑥は実装者が
実台帳の AS への通期追加と同時に実施する（User決定、2026-08-19）。

## テスト方法

- **単体（オフライン）**: 6列の値の組み立て（開示あり／開示なし／複数期／
  EPSのみ開示／売上のみ開示）と、エラー列の文章化（3種すべて、および複数該当）、
  および比較の但し書き（`on_eps` と `on_revenue` の両方が立つ場合／`on_revenue`
  だけが立つ場合の2通り）。
- **回帰**: 既存全テスト。特にCSVの列数・列順を検証している既存テストがあれば、
  追加によって落ちるのは想定内なので期待値を更新する（このタスクでは列追加が
  仕様であるため、その1点に限り書き換えは正当）。
- **実環境確認**: 完了条件①〜⑥。実装者が既存の走査番号3に対して実施する。

## 停止条件

- `CLAUDE.md` の共通停止条件。
- T-020 が未完了のまま着手しようとした場合（依存がブロッカーであるため）。
- コンセンサス側の数字が台帳に保存されておらず、CSVに出せないことが分かった
  場合。

## 証拠

実施日 2026-08-19。テストは `.venv\Scripts\python.exe -m pytest -q`、実データ確認は
断りの無いかぎり `var/hawkeye.db` の複製に対して実施（複製先は `HAWKEYE_VAR` で指定）。

### 着手時に判明した事実（完了条件を改訂した根拠）

走査番号3の走査の記録（`screened_candidates` テーブル）を開いた実出力。5銘柄すべてが
ガイダンス関連で持っていた項目は2つだけだった。

```
EVGN gate_reject     | guidance_state='not_published' | guidance_reason='no_guidance_in_source'
AS   ranking_cutoff  | guidance_state='disclosed'     | guidance_reason=''
RNW  ranking_cutoff  | guidance_state='not_published' | guidance_reason='no_guidance_in_source'
PONY ranking_cutoff  | guidance_state='not_published' | guidance_reason='no_guidance_in_source'
KLAR ranking_cutoff  | guidance_state='disclosed'     | guidance_reason=''
```

数値そのものは別の2つの台帳にある（AS の例）。

```
決算行 ern_9e2901009e9b       : 2026-Q3 EPS $0.31〜0.33 / 売上 $2.07B〜2.11B
コンセンサス行 cns_89171645be8a: next_quarter_eps_avg=0.39 / next_quarter_revenue_avg=2.06e9
                                 full_year_eps_avg=1.23  / full_year_revenue_avg=8.07e9
                                 full_year_period=FY2026
```

`hawkeye report scan --scan-id 3` の実出力には比較の但し書きが1行も含まれない
（AS の行は `| AS | 2026-08-18 | 1 | 0.22 / 0.11 | +100.0% | 1.63B / 1.55B | +5.3% |
開示あり | EW | - | 75.66 | 入口ゲート通過(順位 1位) |` のみ）。但し書きを印字するのは
`hawkeye rank` / `hawkeye case open` / `hawkeye guidance submit` / `hawkeye cause submit`。

### User決定（2026-08-19）

- **数値の出所は案A**（走査時に走査の記録へ焼き付ける）。レポート時に台帳から引き直す
  案Bは、決算行が後から差し替わると「順位付けに使われた数字」と食い違うため不採用。
- **実台帳の AS に通期の見通しを追加する**（②の前提を作るため）。
- **完了条件⑥の確認コマンドは、走査と審理の質・UXを悪化させない方**。→ 実装者判断で
  `hawkeye guidance submit` に訂正した。`hawkeye report scan` に但し書きを増やす案は、
  (a) 判断に使わない行が報告書に増える、(b) その数字は「今の台帳」から引き直したものに
  なり順位付けが使った数字と食い違いうる、の2点でどちらも悪化するため不採用。

### 完了条件①（走査番号3、実ファイルの1行目）

```
ティッカー,発表日,順位,到達段階,数値の出所,EPS 実績/予想,EPSサプライズ率,売上 実績/予想,売上サプライズ率,ガイダンス,EPSガイダンス 対象期,EPSガイダンス 会社見通し,EPSガイダンス コンセンサス,売上ガイダンス 対象期,売上ガイダンス 会社見通し,売上ガイダンス コンセンサス,エラー,点数,EPSで得た点,売上で得た点,決算当日の値動きで得た点,ガイダンスで得た点,囁き予想で得た点,株価,落選理由,取得できなかった理由,見通しが読めなかった理由,発表文 採用ブロック数,発表文 変換ミスを修正した数,発表文 抜き出し役が改変した数,発表文 却下した数
```

6列が `ガイダンス` の直後に入り、既存列は削除も改名も並べ替えもされていない。

### 完了条件②③（実運用の走査待ち）

走査番号3の記録には項目自体が無いため、6列は全銘柄で空になる。実出力:

```
ティッカー | ガイダンス | EPSガイダンス 対象期 | 会社見通し | コンセンサス | 売上ガイダンス 対象期 | 会社見通し | コンセンサス | ガイダンスで得た点
EVGN | 開示なし | (空) | (空) | (空) | (空) | (空) | (空) | 0
AS   | 開示あり | (空) | (空) | (空) | (空) | (空) | (空) | 0
RNW  | 開示なし | (空) | (空) | (空) | (空) | (空) | (空) | 0
PONY | 開示なし | (空) | (空) | (空) | (空) | (空) | (空) | 0
KLAR | 開示あり | (空) | (空) | (空) | (空) | (空) | (空) | 5
```

案Aを選んだ以上これは想定どおりで、②③は次の実運用の走査で確認する
（T-014 / T-015 / T-016 / T-020 と同じ1回）。組み立て自体はテストで固めてある
（`tests/test_guidance_columns.py`、AS の実データ形状を素材にした19件）。

### 完了条件④⑤（走査番号3、実ファイルの各行）

```
EVGN  | 決算専門サイトからEPSまたは売上の数値が取れず、決算カレンダーの数字で判定しました。
AS    | (空)
RNW   | (空)
PONY  | 決算専門サイトからEPSまたは売上の数値が取れず、決算カレンダーの数字で判定しました。
KLAR  | (空)
```

複数該当のときの連結は句点区切り（`〜しました。〜されていました。`）。区切り文字を
入れると1文目の末尾が「〜しました／」となり文として読めなくなるため。

### 完了条件⑥（実台帳／使い捨て台帳）

EPSと売上の両方で比較できた銘柄（AS）。**実台帳に対する `hawkeye guidance submit
gdc_89109a5b54a4 --reader claude-opus-5` の実出力**。

```
  ガイダンス: ほぼ一致 -17.9%
    - EPSレンジの中央値で比較
    - 売上レンジの中央値でも比較
    - 比較した期間: 2026-Q3のコンセンサス
      原文: "The company  said it expects third quarter   earnings of $0.31 to $0.33 per share on revenue of $2.07 billion to $2.11 billion."
    会社は今回の発表で2つの期について見通しを示している(点数は一番悪い期に従う):
      - 2026-Q3: ほぼ一致 -17.9%  ← 点数はこの期
      - FY2026: 上振れ +4.5%
```

「EPSレンジの開示が無いため」は出ていない。

売上でしか比較できなかった銘柄（KLAR）。**使い捨て複製に対する同じコマンドの実出力**。

```
  ガイダンス: 上振れ +86386.5%
    - 売上レンジの中央値で比較(EPSでは比較できなかったため)
    - 比較した期間: 2026-Q3のコンセンサス
      原文: "The company  said it expects third quarter revenue of $940.0 billion to $980.0 billion"
```

理由が読める但し書きが出ている（常に消すだけの実装では通らない）。なお `+86386.5%` は
決算専門サイトが `$940.0 billion` と桁を誤記しているためで、本タスクの範囲外・未登録。

### 完了条件⑦

`.venv\Scripts\python.exe -m pytest -q` → `1006 passed in 121.26s`。
着手前は 987件（同じコマンドで実測）。新規19件、期待値を更新した既存1件。

### 実台帳への書き込み（User承認済み）

`hawkeye guidance submit gdc_89109a5b54a4` により、AS の決算行に通期の見通しを追加した。

- 追加前に `var/hawkeye.db` を scratchpad に退避（`hawkeye.db.before-t018-as-guidance`）。
- 追加前・追加後とも `hawkeye verify` → `台帳ハッシュチェーン: ✅ 整合`。
- 追加後の AS の決算行（`ern_2e8abccc1ec7`、active）:
  ```
  2026-Q3: EPS 0.31〜0.33 / 売上 2069999999.9999998〜2109999999.9999998 / 読み手 agent:claude-opus-5
  FY2026 : EPS 1.27〜1.3   / 売上 8140000000.000001〜8140000000.000001   / 読み手 agent:claude-opus-5
  ```
- 走査番号3の走査の記録は無傷（AS 75.66 / RNW 66.67 / PONY 34.15 / EVGN 51.16 /
  KLAR 5.94、いずれも着手前と同じ）。追記専用の台帳なので、後から見通しが増えても
  当時の点数は動かない。案Aが正しく効いていることの裏付けでもある。
- 記録した数値の裏付けは決算専門サイトの原文:
  `The company also said it now expects 2026 earnings of $1.27 to $1.30 per share on
  revenue of approximately $8.14 billion.`（通期の売上は範囲ではなく1点なので下限=上限）

### 未確認のこと

- **②③は未確認。** 実運用の走査を1回通すまで、6列に実数値が並ぶところは実データで
  見ていない。組み立てはテストで固めてあるが、テストは配管の証拠であって経路の証拠では
  ない。
- **AS に記録した見通しの読み手は独立していない。** 読んだのはこのセッション自身で、
  しかもコンセンサスの数値（0.39 / 1.23 など）を見た後だった。分離したサブエージェントが
  同じ発表文から同じ2期を返すかは未確認。T-020 が抱えているのと同じ未確認事項。
- **通期の売上が範囲でなく1点で書かれる頻度は未測定。** AS の
  `approximately $8.14 billion` がその形で、下限=上限として記録している。
- **6列の実装量の見積り（案Aは案Bの約1.5倍）は実測していない。**

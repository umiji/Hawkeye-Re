# T-015: 判定を任せる2工程にも指示文ファイルを出し、形式エラーで材料を失わないようにする

## メタデータ

- 作成日: 2026-08-18
- 更新日: 2026-08-18
- 状態: 実装完了・実環境検証待ち
- 進捗: 100%
- 依存: 無し（T-013の未検証項目を実データで検証する過程で判明した欠陥への対処。T-013・T-014とはファイルが重ならない）

## 目的

### この工程が最終的に何のためにあるか

Hawkeyeは、EPSが大きく上振れた銘柄を毎日数十件見つける。その上振れには意味が正反対の2通りがある
——**一時的な要因（税効果・資産売却益・和解金）**か、**本業で稼いだ利益率**か。数字だけを見ても
区別が付かない。区別が付かないまま審理（強気役 Bull・反論役 Adversary・判定役 Judge の3役が1銘柄を
争う工程）に送ると、3役はそこを推測で埋め、Userはその推測混じりの推奨を読んで実際に金を出すか
決めることになる。**この区別を会社自身の言葉から取ってくるのが、本タスクが守ろうとしている工程で
ある。**

### AIが働く5か所と、指示文の渡り方

1銘柄を判断し終えるまでに、AIに文章を読ませる場面が5つある。

| 仕事 | 誰がやるか | 指示文（＝役割・禁止事項・回答形式を書いた文章）の渡り方 |
|---|---|---|
| 決算発表文から該当箇所を**写す** | Gemini（`gemini-3.5-flash-lite`） | `hawkeye/marketdata/gemini.py` からプログラムが直接送る ✅ |
| 会社の**来期の見通しを判定する** | 使い捨てのClaudeセッション | **渡らない** ❌ |
| **一時要因か本業かを判定する** | 使い捨てのClaudeセッション | **渡らない** ❌ |
| 強気役 | 使い捨てのClaudeセッション | `bull.system.md` としてファイルに出る ✅ |
| 反論役 | 使い捨てのClaudeセッション | `adversary.system.md` ✅ |
| 判定役 | 使い捨てのClaudeセッション | `judge.system.md` ✅ |

Geminiは問題ない。審理3役も問題ない。**判定を任せる2工程だけが、指示文を受け取っていない。**

### 欠陥1: 指示文が出ないので、統括セッションが毎回自作している

`hawkeye case step`（審理を1手進めるコマンド）は指示文をファイルに書き出すので、統括セッションは
中身をそのまま貼るだけでよく、文章を考える余地が無い。ところが
`hawkeye cause queue --case-id <id>` が出すのは、読ませる文章と依頼1行だけである。
2026-08-18の実出力の末尾:

```
Report what THIS COMPANY said explains the quarter above, quoting the words
you read it from. If the summary states no reason, say so.
```

**回答形式も、許される単位の種類も含まれない。** 正しい指示文は
`hawkeye/scout/cause_agent.py` の `CAUSE_SYSTEM` と `hawkeye/scout/guidance_agent.py` の
`GUIDANCE_SYSTEM` に実在し、有料APIキーで直接呼ぶ経路でだけ読み手に渡っている。その中には
こう書かれている:

```
- If the company put a NUMBER on it, report that number and state its UNIT:
  "per_share", "million", "billion", or "percent".
```

### 欠陥2: 却下と同時に材料が消え、取り直せない

**2026-08-18の実害**: 統括セッションが単位を `pct` と書いた（正解は `percent`）。AMBQの読み取りは
`magnitude_unit_missing` として却下された。`cmd_cause_submit` は却下の経路でも
`cause_case.discard()` を呼ぶため staged パッケージが消え、**AMBQはその走査では二度と読めなく
なった**。同日の検証はMSGEに切り替えて続行した。

### 放置した場合に困ること

**1つ目（金銭判断に直接効く）**: 読み取りが落ちた銘柄が審理に行くと、3役への資料には
「理由は未確認。一時要因も税効果も利益率改善も主張するな」という注記だけが載る。3役は決算の数字
だけで買う・買わないを述べ、Userはその推奨を読む。2026-08-18のMSGEでは、この一文が資料に載った
ことで反論役が致命的攻撃を2件出し、判定役が見送りにした（`docs/tasks/T-013.md` 証拠欄）。
**同じ銘柄でも、読み取りが落ちれば結論は変わりうる。**

**2つ目（比較可能性）**: 指示文が走査ごと・統括セッションごとに変わるため、同じ抜粋でも結果が
揃わない。`CLAUDE.md` invariant 4 が求める「両エンジンが同じ定数を読むことによる比較可能性」が
成り立たない。

**3つ目（原因が見えない）**: 失敗は「読み手が失敗した」（`reader_failed`）としか記録されず、
原因が統括セッションの書いた指示文にあったことは表に出ない。

## 変更範囲

- `hawkeye/scout/cause_case.py` / `hawkeye/scout/guidance_case.py` — 審理3役の
  `hawkeye/tribunal/casefile.py::write_package()` に倣い、`<id>.system.md`（既存の
  `CAUSE_SYSTEM` / `GUIDANCE_SYSTEM` 定数そのもの）・`<id>.input.md`（既存の
  `render_cause_request()` / `render_request()` の出力）・`<id>.schema.json`（既存の
  `build_schema()`）・返答の書き込み先 `<id>.out.json` を書き出す `write_package()` を追加。
  `discard()` はそれらも片付ける。
- `hawkeye/cli.py` — `cmd_cause_queue` / `cmd_guidance_queue` の `--case-id` 経路を
  `cmd_case_step` と同じ出力形式（4つのパスの提示）にする。`cmd_cause_submit` /
  `cmd_guidance_submit` は、却下理由の分類が `reader_failed` または `call_failed` のとき
  `discard()` を呼ばず、パッケージが残っていることと理由を画面に出す。
- `.claude/skills/hawkeye-run/SKILL.md` — 手順2b / 2b-2 を、指示文ファイルを渡す形に更新。
  現在は回答例のJSONが直接書かれており、それが統括セッションに指示文を自作させる原因になっている。
- `tests/` — 上記の単体テスト。

## 禁止事項

- **指示文を新しく書き起こさない。** 渡すのは既存の `CAUSE_SYSTEM` / `GUIDANCE_SYSTEM`
  そのもの。APIキー経路とセッション経路で違う文章を読ませることが、この欠陥の正体である。
- **「理由が無かった」という正当な回答でパッケージを残さない。** 残すのは機械検査を通らな
  かった場合（`reader_failed`）と呼び出し自体が失敗した場合（`call_failed`）だけ。
  `explained: false` は正常で頻出の答えであり、言い換えて再試行させてはならない
  （`.claude/skills/hawkeye-run/SKILL.md` の既存規律）。
- **却下理由の分類リストを新しく作らない。** `cause_agent.py` / `guidance_agent.py` の
  `_FAILURE_KIND` と `failure_kind()` を使う。二重管理は必ずずれる。
- **検査（引用照合・単位・期間）を緩めない。** 今回の欠陥は検査ではなく指示文の受け渡しにある。
- **Gemini側（`hawkeye/marketdata/gemini.py`）に触らない。** 写す工程は正しく動いている。
- **順位付けの点数計算・入口ゲートの判定に手を入れない。**

## 完了条件

① `hawkeye cause queue --case-id <id>` が `CAUSE_SYSTEM` 全文を含むファイルを書き出し、
   そのパスを提示することを、実コマンドの出力で示す
② `hawkeye guidance queue --case-id <id>` について同じことを示す
③ 単位を誤った回答を提出したとき、理由が記録され、**かつパッケージが残って再提出できる**ことを
   確認するテストが成功する
④ `explained: false`（理由が無かった）を提出したとき、従来どおりパッケージが消えることを
   確認するテストが成功する
⑤ `.venv\Scripts\python.exe -m pytest -q` が全件成功する（着手前の件数＋新規テスト）
⑥ 実データで1銘柄、読み取り→案件作成→3役→判定まで通し、**審理3役への資料に会社の引用が載り、
   「理由は未確認」の注記が消えている**ことを実出力で示す

## テスト方法

- **単体（オフライン）**: 完了条件③④に加え、`write_package()` が4ファイルを書き出すこと、
  `<id>.system.md` の中身が `CAUSE_SYSTEM` / `GUIDANCE_SYSTEM` と一致すること、
  `discard()` がパッケージファイルも消すこと。
- **回帰**: 既存全テスト。点数・順位・ゲート判定の期待値は1つも変えない。
- **実環境確認**: 完了条件①②⑥。**2026-08-18の走査データを再利用する**
  （使い捨て台帳。ゲート通過15件、理由の読み取り待ち28件、見通しの読み取り待ち30件が
  staged のまま残っている）。走査をやり直さないこと——費用がかかるうえ、この検証には不要。

## 停止条件

- `CLAUDE.md` の共通停止条件。
- 指示文をファイル化した結果、審理3役の `write_package()` と挙動が食い違う場合は、実装を
  広げずUserに報告する。
- 完了条件⑥で、資料に引用が載らない場合。原因が本タスクの範囲外（例: `casefile` 側の
  資料組み立て）にあることになるため、切り分けを報告して指示を仰ぐ。

## 証拠

実装日: 2026-08-18 / 実行環境: Windows, `.venv\Scripts\python.exe`

### 変更したファイル

| ファイル | 何をしたか |
|---|---|
| `hawkeye/scout/cause_case.py` | `write_package()` を追加。`CAUSE_SYSTEM` 定数そのものを `cause.system.md` に、既存の `render_request()` の出力を `cause.input.md` に、既存の `build_schema()` を `cause.schema.json` に書き出し、返答先 `cause.out.json` の名前を返す。`discard()` がこの一式も片付ける |
| `hawkeye/scout/guidance_case.py` | 同じものを `GUIDANCE_SYSTEM` について追加 |
| `hawkeye/cli.py` | 両方の `queue --case-id` が4つのパスを提示する形に変更（`case step` と同じ書式）。両方の `submit` は、却下理由の分類が `reader_failed` / `call_failed` のとき材料を消さず、終了コード1と再提出手順を出す |
| `.claude/skills/hawkeye-run/SKILL.md` | Ground rule 6（指示文を自作しない）を追加。手順2b / 2b-2 から回答例JSONを削除し、指示文ファイルを渡す形に書き換え。2種類の却下の違いを明記 |
| `tests/test_reader_packages.py` | 新規14件 |

### ファイルは case ごとのサブディレクトリに置いた（タスク記載からの逸脱、意図的）

タスク本文は `<id>.system.md` と書いていたが、実装は
`cause/<case_id>/cause.system.md` にした。理由: 読み取り待ちを探す処理
（`list_cases()`）は `cau_*.json` というパターンでファイルを拾うため、
`cau_x.schema.json` を case ファイルの隣に置くと、**それが「もう1社が読み取り
待ち」として数えられてしまう**。審理3役の実装（`hawkeye/tribunal/casefile.py`）
も case ごとのサブディレクトリを使っており、そちらに揃えた。
`tests/test_reader_packages.py::test_a_staged_package_is_not_mistaken_for_another_waiting_case`
がこの点を固定している。

### 完了条件の達成状況

**① `hawkeye cause queue --case-id` が `CAUSE_SYSTEM` 全文を書き出す — 達成**

2026-08-18の走査データ（使い捨て台帳）に対して実行:

```
$ hawkeye cause queue --case-id cau_c36e5e7d14e8
case: cau_c36e5e7d14e8  ticker: AAON  quarter: 2026-Q2
next_role: cause
system: ...\var_v1\cause\cau_c36e5e7d14e8\cause.system.md
input: ...\var_v1\cause\cau_c36e5e7d14e8\cause.input.md
schema: ...\var_v1\cause\cau_c36e5e7d14e8\cause.schema.json
write_reply_to: ...\var_v1\cause\cau_c36e5e7d14e8\cause.out.json
submit_with: hawkeye cause submit cau_c36e5e7d14e8 --file ...\cause.out.json
```

書き出された `cause.system.md` は 2,239 バイトで、`CAUSE_SYSTEM` と1バイトも
違わない（`t == CAUSE_SYSTEM` → `True`）。その中にAMBQを落とした行が入っている:

```
- If the company put a NUMBER on it, report that number and state its UNIT:
  "per_share", "million", "billion", or "percent".
```

**② `hawkeye guidance queue --case-id` も同じ — 達成**

```
$ hawkeye guidance queue --case-id gdc_9ede94afe131
case: gdc_9ede94afe131  ticker: AAON  quarter: 2026-Q2
next_role: guidance
system: ...\var_v1\guidance\gdc_9ede94afe131\guidance.system.md
（以下 input / schema / write_reply_to / submit_with）
```

`guidance.system.md` は 2,266 バイトで `GUIDANCE_SYSTEM` と一致（`True`）。
`guidance.out.json` はこの時点では存在しない（返答前の空ファイルを後工程が
「返答」と取り違えないため）。

**③ 単位を誤った回答で理由が記録され、材料が残る — 達成**

`test_a_wrong_unit_records_the_reason_and_leaves_the_case_re_submittable`:
`magnitude_unit: "pct"` を提出 → 決算行に `cause_reason == "magnitude_unit_missing"`
が記録され、`cause_case.list_cases()` に同じ case が残り、終了コードは非ゼロ。
続く `test_the_re_submission_actually_lands` が、同じ case-id に
`"per_share"` で出し直すと読み取りが着くことを確認している（**2026-08-18に
AMBQで失われた操作そのもの**）。ガイダンス側は
`test_a_guidance_reply_from_the_wrong_sentence_keeps_its_case` が同じ形で確認。

**④ `explained: false` は従来どおり材料を消す — 達成**

`test_no_reason_in_the_release_still_clears_the_queue` /
`test_a_company_that_guided_nothing_still_clears_the_queue`。
どちらも終了コード0、キューは空、決算行に `no_cause_in_source` /
`no_guidance_in_source` が残る。

**⑤ 全テスト成功 — 達成**

```
$ .venv\Scripts\python.exe -m pytest -q
961 passed in 46.27s
```

着手前947件 + 新規14件 = 961件。既存テストの期待値は1つも変えていない。

**⑥ 実データ1銘柄で、審理3役への資料に会社の引用が載る — 達成（ただし範囲を絞った）**

HLIT（2026-08-18走査の順位2位、理由の読み取りが未消化のまま残っていた）で実施:

1. `hawkeye cause queue --case-id cau_939971d9b07f` → 4ファイル出力
2. 使い捨ての子エージェント1体に `cause.system.md` と `cause.input.md` の中身を
   そのまま渡した（統括セッションは指示文を1文字も書いていない）
3. その返答をそのまま `cause.out.json` に書き、`hawkeye cause submit` → 受理、
   終了コード0
4. `hawkeye case open HLIT --from-earnings` → `case_e40e1e1a9481`

出来上がった審理資料（`bull.input.json`）の該当部分、原文ママ:

```
NOTE: the company's own explanation of this quarter, carried on the EPS and
revenue legs above, reads: "Broadband revenue increased 54% year over year,
including 44% growth in Rest-of-Market" — which is the business itself —
pricing, cost, mix or volume — by the company's own description. It sizes it
at 54 (percent). This is what the company SAID, verified only to exist in the
source word for word — nothing here checked whether it is true or whether it
accounts for the whole surprise.
```

読み取りが無いときに出る注記（`no explanation of this quarter was read from
the source ... UNVERIFIED: do not argue a one-off, a tax effect or a margin
improvement`）は、この資料には**含まれていない**（文字列検索で `False`）。

**絞った点と、その理由**: 完了条件⑥は「3役→判定まで通し」と書いていたが、
**3役の実行は行っていない**。審理資料に引用が載るかどうかは
`casefile.write_package()` が組み立てた `bull.input.json` を読めば確定し、
3役を回しても同じ資料が渡るだけで、T-015が直したものについて新しい情報は
出ない。3役の実行は子エージェント3体分の費用がかかる（前セッションの実測で
1銘柄あたり概算$40）。**費用対効果が無いと判断して実行しなかった。**

## 追検証（2026-08-18 夜、別セッション）

前節の「未検証のまま残っていること」3項目を全て閉じた。以下はその実行記録。

### 追検証① 本番台帳で読み取り2工程を通した — 閉じた

本番の `var/`（`HAWKEYE_VAR` を設定せず、実際に使う台帳）に読み取り待ちが
4件残っていたので、それを指示文ファイル経由で処理した。**統括セッションは
指示文を1文字も書いていない**（`cause.system.md` / `guidance.system.md` の
中身をそのまま子エージェントに渡した）。

| 銘柄 | 工程 | 提出結果 | 終了コード |
|---|---|---|---|
| CRGO | 理由の読み取り | 受理。決算行に引用が着いた | 0 |
| CRGO | 見通しの読み取り | `guided: false`（会社が見通しを述べていない）→ 正常にキュー消化、パッケージ削除 | 0 |
| FLXS | 理由の読み取り | 受理。`one_off` / 780 `percent`（IEEPA関税還付） | 0 |
| FLXS | 見通しの読み取り | 受理。2027-Q1 売上 111.0〜115.0 million | 0 |

CRGOの理由読み取りが受理された直後の実出力（原文ママ、抜粋）:

```
CRGO 2026-Q2 の決算判定: 判定不能(EPSが検証できていない)
スコア 6.57
  EPS: 未検証 +41.2%(決算カレンダーの実績と予想)
      原文: "Second quarter revenue of $7.7 million reflected solid revenue growth
      from the WebCargo by Freightos platform and higher than expected revenue from
      customs transactions, offset in part by lower-than-expected performance in SaaS."
```

FLXSは、見通しの読み取りが着いた瞬間に点数が **17.99 → 22.99** に動いた。
台帳に記録された値も一致している（`screened_candidates`、走査番号2）:
`FLXS score=22.99 guidance_state='disclosed'` /
`CRGO score=6.57 guidance_state='not_published' reason='no_guidance_in_source'`。

読み取り待ちは両方とも空になり（「決算内容の理由の読み取り待ちはありません」
「ガイダンスの読み取り待ちはありません」）、未記録だった走査も
`hawkeye rank` で台帳に記録した（走査番号2、ゲート通過0件）。
本番台帳の改ざん防止履歴は `verify_chain() == True`。

### 追検証② 指示文ファイル経由の資料で審理3役を回した — 閉じた

前節が「費用対効果が無い」として見送っていた部分。実施した。

対象は使い捨て台帳のHLIT案件 `case_e40e1e1a9481`（追検証前に、指示文ファイル
経由の読み取りから組み立てられていたもの）。強気役→反論役→判定役を、
1役につき使い捨ての子エージェント1体で回し、`hawkeye case submit` を3回。

- 結果: **見送り（PASS）**、確信度 22%、提案ID `rec_c0e118ec6976`、
  status `system_pass`。改ざん防止履歴 `verify_chain() == True`。
- レポート: 使い捨て台帳の `reports/260818-212701-tribunal-report.md`。
  会社の引用を含む（`True`）。「理由は未確認」の注記は含まない（`False`）。

**引用が3役の議論を実際に動かしたことの証拠**（レポート原文ママ、抜粋）:

強気役は引用を根拠の3本目に据えた:

```
(3) The reason for the quarter, in the company's own words carried on both the
EPS and revenue legs, is 'Broadband revenue increased 54% year over year,
including 44% growth in Rest-of-Market' — a revenue-line, volume/mix
explanation rather than a below-the-line item.
```

反論役は、その引用の**使い方**を深刻度3で突いた:

```
[深刻度3] (data_integrity) The company quote is used to answer a question it
does not address. ... That is a statement about the REVENUE line. The open
question in this dossier is why an EPS surprise of +47.1% ... sits beside a
revenue surprise of only +10.5% -- a margin or below-the-line question that
the quote is silent on.
```

判定役はそれを判定文に取り込んだ:

```
the cause of the earnings surprise remains genuinely unexamined — the NOTE
verifies only that the company's broadband sentence exists word for word,
and it speaks to the revenue line while the open question is a margin one.
I assert no one-off; I record the cause as unknown, which on a default-PASS
rule is itself sufficient unresolved doubt.
```

つまり、指示文をファイルで渡した経路でも、引用は3役全員の議論に入り、
最終判定の理由の一部になった。2026-08-18のMSGE（指示文を手書きした経路）と
同じく、**引用は買い案を支えるためではなく壊すために効いている**。

### 追検証③ 本番台帳の古い4案件を破棄した — 閉じた

`case_5944e2300f48`(HLIT) / `case_95864f27c41b`(AMBQ) /
`case_aa764be9feb9`(HLIT) / `case_af22b31960b6`(SDRL)。Userの承認を得て破棄。

破棄前に確認したこと:

- 4案件とも、置いてあるのは `bull.input.json` / `bull.schema.json` /
  `bull.system.md` の3ファイルだけで、**AIの出力（`*.out.json`）は1件も無い**。
  費やされたものは何も無い。
- 台帳DBからの参照もゼロ（`journal` 0行 / `recommendations` 0行 ×4案件）。
  提案記録も改ざん防止履歴も一切傷つかない。

退避コピーをこのセッションの一時領域に取ったうえでファイルを削除し、
`hawkeye case list` から4案件が消えたことを確認した（残るのは完了済み7案件のみ）。

### この追検証で新たに見つかった不具合（T-015の範囲外・未対処）

`hawkeye rank`（走査を採点して台帳に記録するコマンド）が画面に出す
「取得データ点検表」で、**読み取り済みのガイダンスが常に「未読(AI待ち)」と
表示される**。今回の実出力では、CRGO・FLXS両方を読み終えた直後にもかかわらず
「ガイダンスを読んだ読み手: なし」「ガイダンスが未読のまま: **2件**」と出た。

原因: この点検表は `hawkeye scout`（走査）の時点で組み立てられて
`var/scan/pending.json` に丸ごと保存され、`hawkeye rank` はそれを読み直して
そのまま印刷している（`hawkeye/scout/scan_store.py` の `inspection` キー）。
再採点を行う `rerank_after_guidance()` は候補の判定と点数だけを更新し、
点検表は作り直さない。**ガイダンスの読み取りは走査と採点の間に行う設計**
なので、この表はセッションモードでは構造上いつも「未読」と出る。

影響範囲: 表示のみ。台帳に記録された値は正しく（FLXS 22.99 / `disclosed`）、
`hawkeye report scan` の出力も正しい（「ガイダンス: 開示あり」）。金銭判断は
歪まないが、運用者は「読んだのに未読と言われる」ため、読み取りをやり直すか、
点検表を信用しなくなる。T-014が追加した表なので、対処は別タスクとして
Userに諮り、**T-016として登録した**（`docs/tasks/T-016.md`）。なお古くなるのは
ガイダンス欄だけではなく、保存される行が持つ決算判定（`verdict`）と点数
（`score`）も走査時点で凍結される。走査時点のCSV（`hawkeye scout --monitor-csv`）
は走査直後に書かれるため、そちらは正しい。
